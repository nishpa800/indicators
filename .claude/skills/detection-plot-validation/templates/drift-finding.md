# Drift finding — `<canonical-name>` — drift #<n>

_Generated by skill `detection-plot-validation` v<version>. Linked from parent report at `docs/validation/<date>-<target-slug>.md`._

## Summary

| Field | Value |
|---|---|
| Drift kind | cosmetic-drift / ipsf-default-variation / ipsf-asymmetry / semantic-drift |
| Locations | <A> ↔ <B> (and ↔ <C> etc. for transitive) |
| Severity | informational / actionable / critical |
| Action required | none / Stage-7 normalization / Stage-7 promote-to-input / immediate user decision |

## Location A

```
File:        <path>
Line range:  <e.g. L78-L82>
Pine variable: <e.g. sigPUP>
Normalized boolean: |
  <the alpha-renamed normalized boolean>
Helpers used:
  <list with file:line refs>
IPSF inputs used:
  <list with parameter names and defaults>
```

## Location B

```
File:        <path>
Line range:  <e.g. L626-L628>
Pine variable: <e.g. det_PUP>
Normalized boolean: |
  <the alpha-renamed normalized boolean>
Helpers used:
  <list>
IPSF inputs used:
  <list>
```

## Side-by-side diff

```diff
--- A: <indicator>::<variable>
+++ B: <indicator>::<variable>
@@ <context>
- <A's boolean line>
+ <B's boolean line>
```

If multiple lines differ, show the full diff. Don't summarize — the reader needs to see the bytes.

## Classification evidence

Why this kind of drift, not another? Cite the decision tree in `references/ipsf-vs-true-drift.md` and the specific predicate that classified it.

Example (cosmetic-drift):

> Predicate matched: "normalized booleans differ ONLY in variable names; operator/operand/shift sequence matches".
> Evidence: A and B both have shape `b1 = i1 AND b2 AND h1 > h2 AND h3 > h4` after alpha-rename. Only the variable names (sigPUP vs det_PUP, pp_priceUp vs an inline expression, etc.) differ.

Example (semantic-drift):

> Predicate matched: "operators / operand structure differ".
> Evidence: A has `csNew3 = csNew1 AND csNew2[1]` (lagged AND); B has `csNew3 = csNew1 AND csNew2` (same-bar AND). The shift index on csNew2 is `[1]` vs `[0]` — meaningfully different. Confirmed by inspecting csNew2's plot offset (offset=0 in both locations, so the `[1]` shift in A genuinely refers to the previous bar's csNew2).

## Proposed action

For the parent report's Phase 4 reconcile table. One of:

- `no-action` — informational only (`identical` / `cosmetic-drift` not worth fixing)
- `unify-variable-name` — rename one side to match the other (Stage-7 cosmetic cleanup)
- `promote-to-input` — hardcoded copy → `input.*` (for `ipsf-asymmetry`)
- `designate-canonical` — pick one as canonical, mark others as `variant_of:` in YAML
- `pine-rename` — Pine variable rename across consumers (requires explicit user approval)
- `pine-source-fix` — fix the boolean math in one location (requires user approval; HIGH RISK)

## User approval needed?

YES / NO

If YES — present this drift artifact to the user and wait for explicit approval before Phase 4 applies anything.

If NO — proceed autonomously per `SKILL.md § "When to ask the user"`.
