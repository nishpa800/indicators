# Canonical decision — `<canonical-name>`

_Generated by skill `detection-plot-validation` v<version> during Phase 4 reconcile. Linked from parent report at `docs/validation/<date>-<target-slug>.md`._

## Decision summary

| Field | Value |
|---|---|
| Target | `<canonical-name>` |
| Canonical owner | `<provenance>` (e.g. `squarify` for Unified Combo) |
| Rationale | <one paragraph> |
| Variant locations | <count> |
| Aliases marked | <count> |
| User approval | timestamp + identifier |

## Why this canonical

State the reasoning explicitly. The canonical decision is the most important output of the validation; readers in the future need to know WHY this location was picked.

Common rationales:

- **Most recent / consolidated**: filename or CHANGELOG marks this as the consolidated source (e.g. HVDPBJPPD's "THE_ONLY_ONE.pine").
- **Highest-coverage**: this location is referenced by the most downstream consumers.
- **Lowest-drift**: this location matches Path A logger output the closest.
- **User-designated**: Anish explicitly named this one as canonical in a chat message or PR comment.
- **Simplest / least-stateful**: when functionality is equivalent, prefer the implementation with fewer dependencies.

If none of the above apply, escalate to user — don't pick on a coin flip.

## Variant locations

Locations that are now marked as `variant_of: <canonical>` in `bible-input/extract-<family>.yaml`:

| Location | Verdict | Action |
|---|---|---|
| <e.g. hvdpbjppd::CS3_BULL> | semantic-drift | mark `variant_of: squarify::UNIFIED_COMBO_BULL` + note the divergence (same-bar AND vs lagged AND) |
| <e.g. b2b-pup::UC_REF> | cosmetic-drift | mark `variant_of: squarify::UNIFIED_COMBO_BULL` + note "alias only, no source action" |
| ... | ... | ... |

## YAML changes proposed

The exact `bible-input/extract-*.yaml` edits to apply. Show before/after for every change.

### File 1: `bible-input/extract-<family>.yaml`

Before:

```yaml
- id: <provenance>::<name>
  ...
```

After:

```yaml
- id: <provenance>::<name>
  variant_of: <canonical>
  variant_kind: <semantic-drift / cosmetic-drift / ipsf-default-variation>
  variant_notes: |
    <one paragraph documenting the divergence>
  ...
```

### File 2: `bible-input/extract-<another-family>.yaml`

(Repeat as needed.)

## Pine source changes proposed?

If YES — list explicitly. PINE SOURCE CHANGES REQUIRE USER APPROVAL.

| File | Line | Before | After | User approved? |
|---|---|---|---|---|
| <path> | L<n> | <Pine line> | <new Pine line> | yes (timestamp + msg-id) / no / pending |

If NO Pine changes: write "_(none — YAML-only reconcile)_".

## Regen plan

Order of operations Phase 4 will run (after user approves the proposal):

1. Apply YAML edits via `Edit` tool to each `bible-input/extract-*.yaml` listed above
2. Run `python3 tools/merge_extracts.py` — regen `data/indicators.yaml` + `data/indicators.json`
3. Run `python3 tools/build_lineage_cards.py` — regen `docs/lineage/*.md`
4. Run `python3 tools/build_docs.py` — regen `docs/{roots,composites,redundancy}.md` + `docs/visual-trees/*.txt` + `INDICATORS_INDEX.md`
5. Verify YAML == JSON byte-equivalent (`merge_extracts.py` asserts this — if it doesn't fail, equal)
6. Commit + push with message referencing this canonical-decision file

## Rollback

If anything goes wrong after step 1: `git checkout HEAD -- bible-input/extract-*.yaml data/indicators.yaml data/indicators.json docs/` to revert YAML + derived docs to pre-decision state.

If a step after #1 fails: stop, investigate, do NOT proceed to the next step.

## User approval record

```
Approved by: <user identifier>
Timestamp: <ISO>
Approval message reference: <chat msg id / PR comment id>
Approval text: |
  <verbatim user approval text>
```
