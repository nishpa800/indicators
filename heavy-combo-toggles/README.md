# Heavy Combo Toggles

Lifts 5 base combos and 15 directional combo booleans **verbatim** from
`heavy-pentagon/versions/HEAVY_PENTAGON_v1.pine` and lumps them into 3
master toggle plots (Heavy Combo Bull/Bear/Neutral).

Stage-1 byte-diff verified the verbatim claim: every helper, threshold,
displacement-engine line, and group-helper boolean is character-identical
to heavy-pentagon's source.

**Canonical file**: `versions/HEAVY_COMBO_TOGGLES_v1.pine` (14.3 KB).

**Parent**: `heavy-pentagon` (canonical RVOL engine + 15 Heavy Combo
composites — see `bible-input/extract-heavy-pentagon.yaml`).

**Bible**: this indicator defines 0 roots and 3 composites. See
`bible-input/extract-heavy-combo-toggles.yaml` for the full extraction.
