# Test Indicators — CHANGELOG

Stage 2 of the indicator bible. Lean Pine indicators that plot ONLY roots,
with every plot named `<provenance>::<root>` so provenance is visible in the
TradingView legend. Anish loads these on TradingView (Stage 3) and visually
confirms each root fires where expected.

## v1 — 2026-05-10

Initial drop. 4 test indicators covering 32 of 195 roots in the bible (the
high-touch root families in Anish's daily optimisation work).

| File                                  | Source canonical                                                                | Roots covered |
|---------------------------------------|---------------------------------------------------------------------------------|---------------|
| `ROOTS_VOLUME_TEST_v1.pine`           | `heavy-pentagon/versions/HEAVY_PENTAGON_v1.pine`                                 | 10 RVOL roots (SAAB, Kratos, RVOL_1X bull/bear, Grand Slam, MOAB, Pentagon, WTC, Hiroshima, Nagasaki) |
| `ROOTS_HVGZIFVG_TEST_v1.pine`         | `hv-fvg-gz1-og/versions/HV_FVG_GZ1_OG_v1.pine`                                   | 6 roots (GZI bull/bear, HV bull/bear, FVG bull/bear raw) |
| `ROOTS_CANDLE_TEST_v1.pine`           | `hvd-pbj-ppd/versions/HVDPBJPPD_2026-05-10_THE_ONLY_ONE.pine` (PUP/PPD/Disp only) | 4 of 8 (PUP, PPD, DISPLACEMENT bull/bear; PBJ/PB deferred — Engine 6 too complex to replicate) |
| `ROOTS_FAUNA_TEST_v1.pine`            | `ultra-combo/versions/ULTRA_COMBO_v57_pine6.pine` (FAUNA block L700-L748)        | 14 roots (MB, RE, TA bull/bear positives + GG, TR, ES, GDR bull/bear excluders) + 2 composites (FAUNA bull/bear locked formula) |

**Total**: 34 root markers + 2 composite markers across 4 indicators. Of the
195 roots in the bible, 32 are exercised by these test indicators directly.
The remaining 163 are exercised by their canonical source indicators when
loaded on TradingView.

**Naming convention**: every `plotshape()` title is `<provenance>::<ROOT>` so
the legend on TradingView reads e.g. `heavy-pentagon::SAAB`.

**Caveat**: every test indicator is a Stage-2 DRAFT generated by Claude.
Verify each plot fires identically to the canonical source BEFORE trusting
in production. Drift findings → `docs/validation-log/`.
