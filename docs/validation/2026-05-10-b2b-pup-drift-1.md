# Drift finding — `B2B_PUP` — drift #1

_Generated by skill `detection-plot-validation` v1.1. Linked from parent report at `docs/validation/2026-05-10-b2b-pup.md`._

## Summary

| Field | Value |
|---|---|
| Drift kind | semantic-drift |
| Locations | b2b-pup (L978) ↔ anish-tb-foster (L421); squarify (L2006) ↔ anish-tb-foster (L421) — transitive |
| Severity | actionable |
| Action required | `variant_of:` designation in YAML; user decision needed to fix or accept missing `conf` gate |

## Location A — b2b-pup canonical

```
File:          b2b-pup/versions/B2B_PUP_Combined_v4.32_2026-05-04.pine
Line range:    L978 (definition); L78 (helper det_PUP)
Pine variable: det_b2bPUP
Normalized boolean:
  b1 = conf AND (close-open)/open*100 > i1 AND volume > ta.highest(h1[1], i2)
  det_b2bPUP = b1 AND nz(b1[1])
  where:
    conf = barstate.isconfirmed
    i1 = input.float(3.0, "% Change", ...)     [IPSF]
    i2 = input.int(10, "Lookback Days", ...)   [IPSF]
    h1 = close<open ? volume : 0.0             [redVol helper]
Helpers used:
  det_PUP at L78 — includes conf gate
IPSF inputs used:
  pp_barSize = input.float(3.0, ...) at L74
  pp_lookback = input.int(10, ...) at L75
```

## Location B — anish-tb-foster variant

```
File:          anish-tb-foster/versions/ANISH_TB_FOSTER_AIRBUD_LEPROSY_ENHANCED_v1.pine
Line range:    L421 (definition); L133 (helper sPPBull); L128-L130 (sub-helpers)
Pine variable: b2bPUP
Normalized boolean:
  b1 = (close-open)/open*100 > i1 AND volume > ta.highest(h1[1], i2)
  b2bPUP = b1 AND b1[1]
  where:
    i1 = barSize parameter (likely input.float, default unknown)
    i2 = ppLookback parameter (likely input.int, default unknown)
    h1 = close<open ? volume : 0.0             [same redVol pattern]
  NOTE: conf (barstate.isconfirmed) is ABSENT from sPPBull
Helpers used:
  sPPBull at L133 — does NOT include conf gate
IPSF inputs used:
  barSize (parameter name and defaults not confirmed without further read)
  ppLookback (parameter name and defaults not confirmed)
```

## Side-by-side diff

```diff
--- A: b2b-pup::det_b2bPUP (L978, helper L78)
+++ B: anish-tb-foster::b2bPUP (L421, helper L133)
@@ boolean structure @@
-b1 = conf AND priceUp AND volume > hiRedVol
+b1 = priceUp AND volume > hiRedVol
 b2bPUP = b1 AND nz(b1[1])
```

The `conf` (`barstate.isconfirmed`) term is present in Location A and absent in Location B.

## Classification evidence

Predicate matched: "operators / operand structure differ."

Evidence: After alpha-rename, Location A's PUP helper has structure `i1 AND h1 AND h2` (three operands: conf, price-pct, volume-gate). Location B's helper has structure `h1 AND h2` (two operands: price-pct, volume-gate only). The missing `conf` operand is not a variable-name cosmetic difference — it is a functional gate. `barstate.isconfirmed` is false on intrabar (real-time mid-bar) updates; its absence means Location B's `b2bPUP` can fire and repaint on non-confirmed bars, while Location A's `det_b2bPUP` cannot.

This is confirmed semantic-drift per `references/ipsf-vs-true-drift.md` § "semantic drift": operand set differs.

## Proposed action

- `variant_of: b2b-pup::B2B_PUP` — anish-tb-foster's `b2bPUP` is a near-match variant with a missing conf gate
- YAML addition to `bible-input/extract-anish-tb-foster.yaml` with drift note
- **User decision needed**: is the missing `conf` intentional (e.g. anish-tb-foster is designed for intrabar live-tick use where confirmed bars are not required) or a copy-paste omission? If omission: fix `sPPBull` at L133 to add `conf and priceUp and volBull`.

## User approval needed?

YES — for any Pine source modification to `anish-tb-foster` to add the `conf` gate. The `variant_of:` YAML addition proceeds autonomously.
