# Drift finding — `SECOND_FLOOR_BULL` — drift #2

_Generated by skill `detection-plot-validation` v1.1. Linked from parent report at `docs/validation/2026-05-10-second-floor.md`._

## Summary

| Field | Value |
|---|---|
| Drift kind | semantic-drift |
| Locations | `hvd-pbj-ppd::SECOND_FLOOR_BULL` (THE_ONLY_ONE, L1276) ↔ `hvd-pbj-ppd-2246::2nd Floor` (HVDPBJPPD_2246, L1625) |
| Severity | actionable |
| Action required | designate-canonical — fire gate differs: THE_ONLY_ONE omits `masterGate` from `fire_Bull2ndFloor`; 2246 uses `floor2_gated` (which adds `oneOfThese + cb1_pass`) vs THE_ONLY_ONE's bare `anyBull2nd` |

## Location A — THE_ONLY_ONE (canonical)

```
File:        hvd-pbj-ppd/versions/HVDPBJPPD_2026-05-10_THE_ONLY_ONE.pine
Line range:  L1242 (anyBull2nd); L1276 (fire_Bull2ndFloor)
Pine variable: fire_Bull2ndFloor
Normalized boolean (fire gate):
  fire_Bull2ndFloor = show_Bull2ndFloor AND anyBull2nd
  (no masterGate in fire line; masterGate applied upstream in aggregator block)
Helpers used:
  anyBull2nd (L1242): conf AND bull_pp AND sigBullPB AND bull_hw_slot
IPSF inputs:
  show_Bull2ndFloor = input.bool(true, ...) [L340]
```

## Location B — HVDPBJPPD_2246 (pending-vetting variant)

```
File:        hvd-pbj-ppd/versions/HVDPBJPPD_2246_FROM_MASTERDIR_2026-05-10.pine
Line range:  L952 (anyBull2nd); L1513 (floor2_gated); L1625 (fire_Bull2ndFloor)
Pine variable: fire_Bull2ndFloor (via floor2_gated)
Normalized boolean (fire gate):
  fire_Bull2ndFloor = show_Bull2ndFloor AND floor2_gated
  floor2_gated = anyBull2nd AND oneOfThese AND cb1_pass
Helpers used:
  anyBull2nd (L952): conf AND bull_pp AND sigBullPB AND bull_hw_slot  [IDENTICAL to A]
  oneOfThese: co-signal cluster gate (same structure as squarify oneOfThese)
  cb1_pass: checkbox Set 1 qualifier filter
IPSF inputs:
  show_Bull2ndFloor = input.bool(true, ...) [L339]
```

## Side-by-side diff

```diff
--- A: hvd-pbj-ppd::SECOND_FLOOR_BULL (THE_ONLY_ONE, fire gate)
+++ B: hvd-pbj-ppd-2246::2nd Floor (2246, fire gate)
@@ fire_Bull2ndFloor @@
- fire_Bull2ndFloor = show_Bull2ndFloor AND anyBull2nd
+ fire_Bull2ndFloor = show_Bull2ndFloor AND (anyBull2nd AND oneOfThese AND cb1_pass)
```

## Classification evidence

Predicate matched: "operators / operand structure differ" → `semantic drift`.

The `anyBull2nd` leaf is byte-identical across both files. The divergence is in the fire gate: the 2246 variant wraps `anyBull2nd` inside `floor2_gated` which adds `oneOfThese AND cb1_pass`. This is architecturally equivalent to Squarify's S5_2F pattern (drift #1), suggesting 2246 is a later version that brought hvd-pbj-ppd in line with squarify's gating philosophy. THE_ONLY_ONE did not receive this update.

Note: 2246 is a pending-vetting variant (not canonical). This drift confirms the 2246 variant is more restrictive than THE_ONLY_ONE (fires less often). If 2246 is eventually promoted to canonical, THE_ONLY_ONE's `fire_Bull2ndFloor` will need the `floor2_gated` wrapper added.

## Proposed action

`variant-of` relationship in YAML: mark `hvd-pbj-ppd-2246::2nd Floor` as a `variant_of: hvd-pbj-ppd::SECOND_FLOOR_BULL` with note "adds oneOfThese+cb1_pass gate (same as squarify S5_2F pattern); pending-vetting — do not promote until TV verified". No Pine changes pending vetting outcome.

## User approval needed?

YES — semantic drift between canonical file and pending-vetting variant. Surfaces the question: when 2246 is vetted, should THE_ONLY_ONE's `fire_Bull2ndFloor` be updated to match the gated form?
