# Drift finding — `ROOFTOP_BEAR` — drift #1

_Generated by skill `detection-plot-validation` v1.1. Linked from parent report at `docs/validation/2026-05-10-rooftop-bear.md`._

## Summary

| Field | Value |
|---|---|
| Drift kind | semantic-drift (on fire_ terminal gate only; core `anyBearRoof` is identical) |
| Locations | hvd-pbj-ppd::ROOFTOP_BEAR (THE_ONLY_ONE L1287) ↔ hvd-pbj-ppd-1939::Rooftop (1939 L1380) |
| Severity | informational — 1939 is pending-vetting, not canonical |
| Action required | designate-canonical; mark 1939 as `variant_of` in YAML |

## Location A — canonical

```
File:          hvd-pbj-ppd/versions/HVDPBJPPD_2026-05-10_THE_ONLY_ONE.pine
Line range:    L1287
Pine variable: fire_BearRooftop
Normalized:    show_BearRooftop AND anyBearRoof
IPSF inputs:   show_BearRooftop = input.bool(true, "Rooftop", group=grp_t2)
Helpers:       anyBearRoof (L1243) = conf AND bear_pp AND sigBearPBJ AND bear_hw_slot
```

## Location B — 1939 masterdir variant

```
File:          hvd-pbj-ppd/versions/HVDPBJPPD_1939_FROM_MASTERDIR_2026-05-10.pine
Line range:    L1380
Pine variable: fire_BearRooftop
Normalized:    show_BearRooftop AND anyBearRoof AND masterGate
IPSF inputs:   show_BearRooftop = input.bool(true, "Rooftop", group=grp_t2)
Helpers:       anyBearRoof (L979) = conf AND bear_pp AND sigBearPBJ AND bear_hw_slot [identical to A]
               masterGate (L97)   = NOT en_firstBarOnly OR (isFirstBar AND _anyHV_bar0)
```

## Side-by-side diff

```diff
--- A: hvd-pbj-ppd::ROOFTOP_BEAR (THE_ONLY_ONE L1287)
+++ B: hvd-pbj-ppd-1939::Rooftop (1939 L1380)
@@ fire_ terminal boolean
- bool fire_BearRooftop=show_BearRooftop and anyBearRoof
+ bool fire_BearRooftop=show_BearRooftop and anyBearRoof and masterGate
```

`masterGate` definition (L97 in 1939):
```pine
bool masterGate = not en_firstBarOnly or (isFirstBar and _anyHV_bar0)
```
This gates every fire_ boolean in 1939 to fire only on the first bar of the day when `en_firstBarOnly` is true AND there is an HV bar on that day. It is a session-scope suppression gate applied uniformly across the entire 1939 file — not specific to Rooftop.

## Classification evidence

Predicate matched: "operators / operand structure differ" → `semantic drift`.

The normalized form of the fire_ terminal differs by one additional operand (`masterGate`) in location B. After normalization, Location A = `b1 AND b2`; Location B = `b1 AND b2 AND b3` where `b3 = masterGate`. These are NOT structurally equivalent — Location B will suppress fires that Location A would emit on non-first-bars-of-day when `en_firstBarOnly=true`.

This is expected behavior for the 1939 experiment (all fire_ booleans in 1939 add `and masterGate`; it is a file-wide gate, not a Rooftop-specific drift). The core detection (`anyBearRoof`) is `identical` — only the display gate differs.

## Proposed action

`designate-canonical` — THE_ONLY_ONE is canonical (per MANIFEST.md). Mark `hvd-pbj-ppd-1939::Rooftop` as `variant_of: hvd-pbj-ppd::ROOFTOP_BEAR` in `bible-input/extract-hvd-pbj-ppd-1939.yaml` (or equivalent pending-vetting extract). No Pine source change needed. No action on THE_ONLY_ONE or squarify.

## User approval needed?

NO — per `SKILL.md § "When to ask the user"`: semantic-drift findings are handled autonomously by creating a new versioned documentation file (this artifact) and marking the non-canonical as `variant_of`. No Pine source rename or deletion is involved. 1939 is pending-vetting, so the variant tag is consistent with its lifecycle stage.
