# Drift finding — `SECOND_FLOOR_BULL` — drift #1

_Generated by skill `detection-plot-validation` v1.1. Linked from parent report at `docs/validation/2026-05-10-second-floor.md`._

## Summary

| Field | Value |
|---|---|
| Drift kind | semantic-drift |
| Locations | `hvd-pbj-ppd::SECOND_FLOOR_BULL` (THE_ONLY_ONE) ↔ `squarify::S5_2F` |
| Severity | actionable |
| Action required | designate-canonical — squarify adds `oneOfThese + cb1_pass + NOT floor_sq` gate that is absent in hvd-pbj-ppd; user must confirm which is authoritative for alert/scoring purposes |

## Location A — hvd-pbj-ppd (THE_ONLY_ONE)

```
File:        hvd-pbj-ppd/versions/HVDPBJPPD_2026-05-10_THE_ONLY_ONE.pine
Line range:  L1239-L1242 (helpers L1239-L1240; anyBull2nd L1242)
             fire_Bull2ndFloor L1276; plot L1359
Pine variable: anyBull2nd (exposed as fire_Bull2ndFloor / SECOND_FLOOR_BULL)
Normalized boolean:
  b1 = conf AND bull_pp AND sigBullPB AND bull_hw_slot
Helpers used:
  bull_hw_slot (L1239): sigBullRVOL1x or sigGrandSlam or sigWTC or sigHiroshima or (sigNagasaki and nag_dir_bull)
  nag_dir_bull (L1229): sigBullRVOL1x or sigGrandSlam or sigFAUNABull or sigDISPBull or sigBullPBJ or sigPUP or gz_bullHV or gz_bullGZI
IPSF inputs used:
  show_Bull2ndFloor = input.bool(true, "2nd Floor", ...)  [L340 — gates fire_Bull2ndFloor only]
```

## Location B — squarify (S5_2F)

```
File:        squarify/versions/SQUARIFY_46_v2_2026-05-04.pine
Line range:  L1352 (anyBull2nd reference); L2157 (floor2_sq def); L2231 (plot)
Pine variable: floor2_sq (exposed as S5_2F)
Normalized boolean:
  b1 = anyBull2nd AND oneOfThese AND cb1_pass AND NOT floor_sq
     = (conf AND bull_pp AND sigBullPB AND bull_hw_slot) AND oneOfThese AND cb1_pass AND NOT (anyBullFloor AND oneOfThese AND cb1_pass_floor)
Helpers used:
  anyBull2nd (L1352): conf AND bull_pp AND sigBullPB AND bull_hw_slot  [identical to Location A]
  oneOfThese (L2109): sigP21BullUUUU OR sigP21BullUUU OR sigUUBull OR sigOmegaLongA OR det_bullNapalmCons OR det_b2bBullNapalm OR csNew3_Bull OR (csNew1_Bull OR csNew2_Bull) OR sigWMD
  cb1_pass (L2136): f_cb(cb1_uu, cb1_combo, cb1_d9, cb1_pup, cb1_pbj, cb1_l1, cb1_wmd) — user-configurable checkbox Set 1
  floor_sq (L2156): anyBullFloor AND oneOfThese AND cb1_pass_floor  [suppresses when FLOOR also fires]
  cb1_pass_floor: f_cb(..., false, ...) — same as cb1_pass but PBJ checkbox excluded
IPSF inputs used:
  en_5 = input.bool(true, "5. 2F", ...)  [L140 — gates plotshape]
  cb1_* = multiple input.bool(...) — checkbox Set 1 qualifiers (user-configurable)
```

## Side-by-side diff

```diff
--- A: hvd-pbj-ppd::SECOND_FLOOR_BULL (anyBull2nd)
+++ B: squarify::S5_2F (floor2_sq)
@@ boolean expression @@
- b1 = conf AND bull_pp AND sigBullPB AND bull_hw_slot
+ b1 = (conf AND bull_pp AND sigBullPB AND bull_hw_slot) AND oneOfThese AND cb1_pass AND NOT floor_sq
```

Squarify adds two extra AND gates:
1. `oneOfThese` — requires co-presence of at least one of: UUUU/UUU/UU/OmegaA/Napalm/B2B-Napalm/Combo/WMD
2. `cb1_pass` — user-configurable checkbox Set 1 qualifier filter
3. `NOT floor_sq` — explicit suppression when FLOOR (S4) also fires same bar

## Classification evidence

Predicate matched: "operators / operand structure differ" → `semantic drift`.

Evidence: After alpha-rename, Location A normalizes to `b1 = i1 AND b2 AND b3 AND b4` (4 operands: conf, bull_pp, PB, hw_slot). Location B normalizes to the same 4 operands PLUS two additional AND clauses (`oneOfThese`, `cb1_pass`) and a NOT clause (`floor_sq`). The extra operands are not variable-name differences — they represent genuinely different Boolean logic. A bar where `anyBull2nd` is true but `oneOfThese` is false would fire in Location A but NOT in Location B. Confirmed by reading `oneOfThese` at L2109: it gates on a co-occurring signal cluster that is absent from Location A's logic entirely.

This is NOT IPSF asymmetry: the extra operands in Location B are not simply an un-promoted hardcoded constant — they are structurally new AND clauses.

## Proposed action

`designate-canonical` — present both implementations to user:
- Location A (`hvd-pbj-ppd::SECOND_FLOOR_BULL`) is the raw/ungated form: fires whenever PP bull + PB + HW-volume slot are true, regardless of co-occurring signals.
- Location B (`squarify::S5_2F`) is a filtered/gated form: additionally requires a co-signal cluster (oneOfThese) + checkbox qualifier + FLOOR suppression.

Recommend: mark `squarify::S5_2F` as `variant_of: hvd-pbj-ppd::SECOND_FLOOR_BULL` with note "adds oneOfThese + cb1_pass + NOT floor_sq gate". No Pine source change needed — the variant relationship is the correct characterization. YAML update only.

## User approval needed?

YES — semantic drift between two different composites. User must confirm:
1. Which is the "authoritative" 2F for alert purposes? (hvd-pbj-ppd ungated vs squarify gated)
2. Should hvd-pbj-ppd add the `oneOfThese + NOT floor` suppression to match squarify? Or is the ungated version intentional?
