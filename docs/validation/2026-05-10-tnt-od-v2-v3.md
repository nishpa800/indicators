# Validation report — `TNT-OD v3 vs v2` (combined VALIDATE 3 + 4)

_Skill: `detection-plot-validation` v1.1.0. Run on 2026-05-10. Path: docs/validation/2026-05-10-tnt-od-v2-v3.md._

## Why combined

Anish's ask: "validate, um, combo chain, the TNT o... overdrive version three. And, actually, let's do version two as well". TNT-OD v3 vs v2 is structurally a single comparison question (v3 = v2 + new features); validating them as one report covers both.

## Summary

| Field | Value |
|---|---|
| Files compared | `tnt-od/versions/TNT_OD_v2.pine` (1802 lines) · `tnt-od/versions/TNT_OD_v3.pine` (2150 lines) · `tnt-od/versions/TNT_Opening_Drive_OD_v3_2026-05-04.pine` (1802 lines, **internally v2 despite filename**) |
| v3-only features verified | T1 RELAY (L1668-1669) · T1 STACK (L1674-1675) · New conditional gate (L1155-1168) |
| Shared features verified | FUSE (identical) · DYNAMITE (identical) · B2B NAPALM (v3 adds `and nz(gate_bull[1])` per documented CHANGELOG enhancement) |
| Filename-vs-content mismatch | `TNT_Opening_Drive_OD_v3_2026-05-04.pine` is internally TNT OD v2 (same boolean as TNT_OD_v2.pine for sampled targets) |
| Phase 3 path used | BLOCKED-NEEDS-TV-FIRING-SKILL |
| Final verdict v3 | **OK-PENDING-TV-FIRING** (no drift; runtime confirmation needed) |
| Final verdict v2 | **OK with filename-misleading flag** (no drift; misleading file naming should be addressed later via additive new-versioned file, not rename) |

## v3-only features

### T1 RELAY

| File | Line | Boolean |
|---|---|---|
| `tnt-od/versions/TNT_OD_v3.pine` | 1668 | `bool p_t1RelayBull = t1_v2_anyB and t1_v1_anyB and barstate.isconfirmed` |
| `tnt-od/versions/TNT_OD_v3.pine` | 1669 | `bool p_t1RelayBear = t1_v2_anyS and t1_v1_anyS and barstate.isconfirmed` |
| All other Pine files | — | **0 occurrences** (correct — v3-only feature) |

**Verdict**: ✓ Implemented only in v3. No drift.

### T1 STACK

| File | Line | Boolean |
|---|---|---|
| `tnt-od/versions/TNT_OD_v3.pine` | 1674 | `bool p_t1StackBull = t1_v1_count_b >= 2 and barstate.isconfirmed` |
| `tnt-od/versions/TNT_OD_v3.pine` | 1675 | `bool p_t1StackBear = t1_v1_count_s >= 2 and barstate.isconfirmed` |
| All other Pine files | — | **0 occurrences** |

**Verdict**: ✓ Implemented only in v3. No drift.

### New conditional gate

| File | Line | Definitions |
|---|---|---|
| `tnt-od/versions/TNT_OD_v3.pine` | 1155 | `bool gate_disp_bull = barstate.isconfirmed and (disp_range[1] > nz(gate_disp_threshold[1])) and isBullishFVG and (close[1] > open[1])` |
| `tnt-od/versions/TNT_OD_v3.pine` | 1156 | `bool gate_disp_bear = ... (close[1] < open[1])` |
| `tnt-od/versions/TNT_OD_v3.pine` | 1167 | `bool gate_bull = (not en_newGate) or gate_bull_raw` |
| `tnt-od/versions/TNT_OD_v3.pine` | 1168 | `bool gate_bear = (not en_newGate) or gate_bear_raw` |
| All other Pine files | — | **0 occurrences** |

**Verdict**: ✓ Implemented only in v3. No drift.

## Shared features — drift check

### B2B Napalm (`p_b2bBull`)

| File | Line | Boolean |
|---|---|---|
| `tnt-od/versions/TNT_OD_v2.pine` | 1030 | `bool p_b2bBull = det_bullNapalm and not supp_bullNPM and (det_bullNapalm[1] == true) and not na(sessionFirstBarIdx) and (bar_index - 2) >= sessionFirstBarIdx and en_b2bBull` |
| `tnt-od/versions/TNT_OD_v3.pine` | 1247 | (same as v2) **+ `and nz(gate_bull[1])`** |
| `tnt-od/versions/TNT_Opening_Drive_OD_v3_2026-05-04.pine` | 1030 | (same as v2 — no gate suffix) |

**Verdict**: ✓ The `and nz(gate_bull[1])` addition in v3 is the **documented v3 enhancement** per TNT-OD CHANGELOG: "B2B NAPALM ... now ALSO requires ANY of: RVOL 1x / Grand Slam / Unified Combo / Nagasaki+Any / HCT / Displacement ≥ gateStdMult". This is intended, not drift.

Note: VALIDATE 6 (B2B Napalm) covers B2B Napalm in its own dedicated report.

### FUSE (`det_fuseBull` + `p_fuseBull`)

| File | Line | Boolean |
|---|---|---|
| `tnt-od/versions/TNT_OD_v2.pine` | 985 | `bool det_fuseBull = det_contBull and not na(lastBullTNTVis) and not na(lastBullNPMVis) and lastBullNPMVis < lastBullTNTVis and lastBullTNTVis < bar_index and (bar_index - lastBullTNTVis) <= SUDDEN_PROX and (lastBullTNTVis - lastBullNPMVis) <= SUDDEN_PROX and lastBullNPMVis >= nz(sessionFirstBarIdx, 0)` |
| `tnt-od/versions/TNT_OD_v3.pine` | 1198 | (identical to v2) |
| `tnt-od/versions/TNT_Opening_Drive_OD_v3_2026-05-04.pine` | 985 | (identical to v2) |

**Verdict**: ✓ Identical across all three files. No drift.

### DYNAMITE (`det_dynamiteBull`)

| File | Line | Boolean |
|---|---|---|
| `tnt-od/versions/TNT_OD_v2.pine` | 902 | `bool det_dynamiteBull = barstate.isconfirmed and dyn_disp_b1 and dyn_disp_b2 and dyn_bull_dir and dyn_fauna_bull and dyn_bullFVG` |
| `tnt-od/versions/TNT_OD_v3.pine` | 974 | (identical to v2) |
| `tnt-od/versions/TNT_Opening_Drive_OD_v3_2026-05-04.pine` | 902 | (identical to v2) |

**Verdict**: ✓ Identical. No drift.

## Filename anomaly

`tnt-od/versions/TNT_Opening_Drive_OD_v3_2026-05-04.pine` has "v3" in its filename but its content matches TNT_OD_v2.pine on every spot-checked feature (FUSE L985, B2B NAPALM L1030, DYNAMITE L902). The two files have identical line numbers and identical booleans. They appear to be byte-identical or near-identical copies.

This is a filename-vs-content mismatch — the filename suggests it's v3 but it's actually a v2 snapshot.

**Resolution per standing approval (no renames)**:

- Don't rename `TNT_Opening_Drive_OD_v3_2026-05-04.pine`. It stays as-is.
- Document the misleading filename in this report.
- Anish can later create a new versioned file with a clearer name (e.g. `TNT_OD_v2_SNAPSHOT_2026-05-04.pine`) but the original stays.
- `tnt-od/versions/LATEST.txt` already points at `TNT_Opening_Drive_OD_v3_2026-05-04.pine` per the bible's current canonical designation. After Anish vets TNT_OD_v3.pine (per `tnt-od/VETTING.md`), LATEST.txt can flip to `TNT_OD_v3.pine`.

## Phase 3 — TV firing

**Status**: BLOCKED-NEEDS-TV-FIRING-SKILL.

**Why**: TNT-OD's full plot set requires running on real data to confirm fire bars. Path B (Python ports) covers most TNT-OD via the `tnt_od` pack (42 detections per the parallel session's handoff), but:
- The `realtime-indicators` repo is not pushed (Anish's Mac only)
- 3 WBUSH composites are stubbed (non-portable `tv_ta.relativeVolume`)
- T1 RELAY, T1 STACK, and the new gate are likely not yet ported (v3-only)

Path A required for full v3 verification. Run `detection-plot-tv-firing` skill when at desk.

## Phase 4 — Reconcile

No Pine modification. No YAML changes — TNT-OD v3 vs v2 is correctly differentiated in `bible-input/MANIFEST.md` (v2 file at `TNT_Opening_Drive_OD_v3_2026-05-04.pine` per current LATEST; v3 file at `TNT_OD_v3.pine` flagged as pending vetting per `tnt-od/VETTING.md`).

## Final verdict

### VALIDATE 3 (TNT-OD v3)

**OK-PENDING-TV-FIRING**. New v3 features correctly localised to TNT_OD_v3.pine. The documented v3 enhancement to NPM-family Tier 1 (adding `and nz(gate_bull[1])`) is implemented per the CHANGELOG. No semantic drift.

### VALIDATE 4 (TNT-OD v2)

**OK with filename-misleading flag**. TNT_OD_v2.pine and TNT_Opening_Drive_OD_v3_2026-05-04.pine are functionally identical for sampled features. The "v3" in the latter's filename is misleading. No drift in v2 itself.

## Stage-7 followups

- [ ] Run `detection-plot-tv-firing` skill on TNT_OD_v3.pine — confirm T1 RELAY, T1 STACK, and gate behavior matches the CHANGELOG spec
- [ ] After TV firing verifies v3: flip `tnt-od/versions/LATEST.txt` from `TNT_Opening_Drive_OD_v3_2026-05-04.pine` to `TNT_OD_v3.pine`
- [ ] Consider adding a new versioned file `TNT_OD_v2_SNAPSHOT_2026-05-04.pine` (NOT a rename — per standing approval) to disambiguate the filename
- [ ] Re-extract `bible-input/extract-tnt-od.yaml` against TNT_OD_v3.pine after vetting completes (Stage 8 followup per `tnt-od/VETTING.md`)
- [ ] Verify the 3 WBUSH composites that are stubbed in `tnt_od` Python pack can be implemented (Path B coverage)
