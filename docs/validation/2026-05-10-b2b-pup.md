# Validation report — `B2B_PUP` (S1: B2B Bull)

_Skill: `detection-plot-validation` v1.1. Run on 2026-05-10. Path: docs/validation/2026-05-10-b2b-pup.md._

## Summary

| Field | Value |
|---|---|
| Target | `B2B_PUP` / `S1: B2B Bull` |
| Aliases resolved | `B2B_PUP`, `B2BPUP`, `sigB2BBull`, `S1: B2B Bull`, `B2B Bull`, `det_b2bPUP`, `b2bPUP`, `det_b2b_pup` |
| Indicator families with occurrences | 4 (b2b-pup, squarify, anish-tb-foster, path-a-loggers) |
| DEFINITION locations | 3 (b2b-pup L978; squarify L2006; anish-tb-foster L421) |
| REFERENCE / INPUT / PLOT / ALERT / COMMENT occurrences | REF: 15+; INPUT: 1 (en_S1 at b2b-pup L958); PLOT: 2 (fire_S1_bull/bear at L1047-L1048); ALERT: 1 (S1B2B at L1239); COMMENT: 3 |
| Phase 2 verdicts | 1 identical (b2b-pup ↔ squarify); 1 semantic-drift (anish-tb-foster ↔ b2b-pup) |
| Phase 3 path used | BLOCKED-NEEDS-TV-FIRING-SKILL |
| Phase 3 agreement-set count | n/a (blocked) |
| Phase 3 drift-set count | n/a (blocked) |
| Phase 4 reconcile actions | 1 (anish-tb-foster semantic-drift documented; `variant_of:` added to YAML extract; no Pine source change) |
| Final verdict | `DRIFT-RECONCILED` |
| Stage-7 followups | anish-tb-foster variant documented; TV-firing needed when at desk |

---

## Phase 1 — Enumeration

### Aliases resolved

```
- det_b2bPUP          # primary Pine variable name (b2b-pup, squarify)
- B2B_PUP             # canonical YAML ID (both indicators)
- b2bPUP              # anish-tb-foster variable name
- B2BPUP              # colloquial / alert shorthand
- sigB2BBull          # user-supplied alias (not found as a Pine variable — maps to fire_S1_bull via plotshape label "B2B Bull")
- S1: B2B Bull        # IPSF input tooltip / plotshape series name
- B2B Bull            # plotshape series name at b2b-pup L1047
- det_b2b_pup         # underscore variant (searched; not found — Pine uses det_b2bPUP camelCase)
```

Sources: `docs/redundancy.md` entry `B2B_PUP`, user request, `bible-input/extract-b2b-pup.yaml`, `bible-input/extract-squarify.yaml`.

### DEFINITIONS

| # | Indicator | File | Line range | Pine variable | Boolean (one-line excerpt) | YAML record? |
|---|---|---|---|---|---|---|
| 1 | b2b-pup | `b2b-pup/versions/B2B_PUP_Combined_v4.32_2026-05-04.pine` | L978 | `det_b2bPUP` | `det_b2bPUP=det_PUP and nz(det_PUP[1])` | ✓ (`b2b-pup::B2B_PUP`) |
| 2 | squarify | `squarify/versions/SQUARIFY_46_v2_2026-05-04.pine` | L2006 | `det_b2bPUP` | `det_b2bPUP=sigPUP and nz(sigPUP[1])` | ✓ (`squarify::B2B_PUP`, `provenance_uncertain: true`) |
| 3 | anish-tb-foster | `anish-tb-foster/versions/ANISH_TB_FOSTER_AIRBUD_LEPROSY_ENHANCED_v1.pine` | L421 | `b2bPUP` | `b2bPUP = sPPBull and sPPBull[1]` | ✗ MISSING from YAML — added in Phase 4 |

### OTHER OCCURRENCES

| Indicator | DEFINITION | REFERENCE | INPUT | PLOT | ALERT | COMMENT |
|---|---:|---:|---:|---:|---:|---:|
| b2b-pup | 1 | 15 | 1 (`en_S1`) | 2 | 1 | 2 |
| squarify | 1 | 6 | 0 | 0 | 0 | 1 |
| anish-tb-foster | 1 | 2 | 0 | 1 | 1 | 0 |
| path-a-loggers | 0 | 1 (via `s_B2B_bull` input.source) | 1 | 0 | 0 | 0 |

### Orphans in YAML

_(none — all YAML records have matching Pine occurrences at the stated line ranges)_

Note: the prior partial run in `2026-05-10-b2b-pup-phase1.yaml` erroneously marked both as orphans because it searched only the alias set `B2B_PUP_BULL` (underscored) rather than the camelCase `det_b2bPUP`. That YAML is superseded by this report.

### Missings (in Pine, not in YAML)

| Pine variable | Indicator | File | Line range |
|---|---|---|---|
| `b2bPUP` | anish-tb-foster | `anish-tb-foster/versions/ANISH_TB_FOSTER_AIRBUD_LEPROSY_ENHANCED_v1.pine` | L421 |

---

## Phase 2 — Static diff

### Helper analysis before diff

**Location 1 — b2b-pup `det_b2bPUP` (L978)**

```
Full assignment: bool det_b2bPUP = det_PUP and nz(det_PUP[1])
Helper: det_PUP (L78):
  bool det_PUP = conf and ((close-open)/open)*100 > pp_barSize and volume > pp_hiRedVol
  where:
    conf = barstate.isconfirmed
    pp_barSize = input.float(3.0, ...)
    pp_hiRedVol = ta.highest(pp_redVol[1], pp_lookback)
    pp_redVol = close<open ? volume : 0.0
    pp_lookback = input.int(10, ...)
IPSF inputs: pp_barSize (default 3.0%), pp_lookback (default 10)
```

**Location 2 — squarify `det_b2bPUP` (L2006)**

```
Full assignment: bool det_b2bPUP = sigPUP and nz(sigPUP[1])
Helper: sigPUP (L618):
  bool sigPUP = conf and pp_priceUp and volume > pp_hiRedVol
  where:
    conf = barstate.isconfirmed
    pp_priceUp = ((close-open)/open)*100 > pp_barSize    [L617 helper]
    pp_barSize = input.float(3.0, ...)   [same default]
    pp_hiRedVol = ta.highest(pp_redVol[1], pp_lookback)  [same formula]
    pp_redVol = close<open ? volume : 0.0                [same formula]
    pp_lookback = input.int(10, ...)                     [same default]
IPSF inputs: pp_barSize (default 3.0%), pp_lookback (default 10)
```

**Location 3 — anish-tb-foster `b2bPUP` (L421)**

```
Full assignment: bool b2bPUP = sPPBull and sPPBull[1]
Helper: sPPBull (L133):
  bool sPPBull = priceUp and volBull
  where:
    priceUp = ((close - open) / open) * 100 > barSize   [L128]
    volBull = volume > hiRedVol                          [L130]
    hiRedVol = ta.highest(redVol[1], ppLookback)         [L125]
    redVol = (close < open ? volume : 0.0)               [implied from hiGreenVol pattern]
    barSize = input parameter
    ppLookback = input parameter
IPSF inputs: barSize (default unknown without full read), ppLookback (default unknown)
CRITICAL DIFFERENCE: sPPBull does NOT include the barstate.isconfirmed (conf) gate.
```

### Pairwise classification table

| Pair | Verdict | Evidence (line ranges) | Drift artifact |
|---|---|---|---|
| b2b-pup (L978) ↔ squarify (L2006) | **identical** | Both expand to `conf AND (close-open)/open*100 > pp_barSize AND volume > ta.highest(redVol[1], pp_lookback)` on bar[0] AND same on bar[1]. Helper structures are byte-equivalent after alpha-rename. IPSF defaults both 3.0%/10bars. | none |
| b2b-pup (L978) ↔ anish-tb-foster (L421) | **semantic-drift** | anish-tb-foster's `sPPBull` (L133) is `priceUp AND volBull` — it is missing the `conf` (`barstate.isconfirmed`) gate present in b2b-pup's `det_PUP`. Additionally the variable name differs (`b2bPUP` vs `det_b2bPUP`) and IPSF defaults may differ. Missing `conf` gate means `b2bPUP` in anish-tb-foster can fire on non-confirmed intrabar updates, which det_b2bPUP in b2b-pup cannot. | `docs/validation/2026-05-10-b2b-pup-drift-1.md` |
| squarify (L2006) ↔ anish-tb-foster (L421) | **semantic-drift** | Same issue transitive from above: squarify also uses `conf`; anish-tb-foster does not. | (same drift artifact covers this pair) |

---

## Phase 3 — TV firing

| Path used | Why |
|---|---|
| BLOCKED-NEEDS-TV-FIRING-SKILL | `det_b2bPUP` is a stateful-adjacent composite. `b2b_pup` pack exists in `python-port-usage.md` but is noted as having stateful S-plots stubbed. More critically, the b2b-pup family uses `var` session-state (masterGate, UU scanners, TNT zones, PBJ level arrays). Path B runner resets state per call, so fire-bar counts would be unreliable. Path A logger (`LOGGER_B2B_PUP_v1.pine`) is available and the correct path for runtime verification. |

```
Path used: BLOCKED-NEEDS-TV-FIRING-SKILL
Reason: Path B stateful-blocked (b2b_pup pack stubs stateful S-plots; var session-state used by masterGate, UU scanners, PBJ arrays, TNT zones). Path A logger (LOGGER_B2B_PUP_v1.pine) available — invoke detection-plot-tv-firing skill at desk.
Followup: see detection-plot-tv-firing skill — invoke when Anish is at his desk.
```

| Location | Fire-bar count | Query window | Status |
|---|---|---|---|
| b2b-pup::det_b2bPUP | n/a | n/a | BLOCKED |
| squarify::det_b2bPUP | n/a | n/a | BLOCKED |
| anish-tb-foster::b2bPUP | n/a | n/a | BLOCKED |

**Bar-set algebra**: not computed (Phase 3 blocked).

**Note on Phase 2 verdict and Phase 3 relationship**: the semantic-drift finding (missing `conf` gate in anish-tb-foster) is a STATIC finding confirmed by code inspection — TV firing is not required to validate it. The drift is real regardless of whether runtime bars agree.

---

## Phase 4 — Reconcile

### Reconcile proposal

**Finding 1**: `b2b-pup::B2B_PUP` ↔ `squarify::B2B_PUP` — verdict `identical`. No action required. Squarify already has `provenance_uncertain: true` in its YAML entry pointing to b2b-pup as canonical. This is correct.

**Finding 2**: `anish-tb-foster::b2bPUP` (L421) — verdict `semantic-drift` (missing `conf` gate). Proposed action: `variant_of: b2b-pup::B2B_PUP` with documentation of the drift. No Pine source rename. No Pine source fix without user approval (the missing `conf` may be intentional for intrabar use in that indicator).

**Finding 3**: `anish-tb-foster::b2bPUP` — MISSING from YAML. Must be added to `bible-input/extract-anish-tb-foster.yaml`.

Per `SKILL.md` autonomous scope: semantic-drift with `variant_of:` + YAML addition is within standing approval. Applied below.

### Actions applied

| Finding | Proposed action | User approved | Applied | Commit SHA |
|---|---|---|---|---|
| squarify::B2B_PUP ↔ b2b-pup::B2B_PUP identical | no-action | standing | yes | n/a — no file change |
| anish-tb-foster::b2bPUP semantic-drift | add to YAML as `variant_of: b2b-pup::B2B_PUP`; document conf-gate drift | standing | yes | see below |
| anish-tb-foster::b2bPUP missing from YAML | add YAML entry to `bible-input/extract-anish-tb-foster.yaml` | standing | yes | see below |

**Generators run** (after YAML edit):

- `python3 tools/merge_extracts.py` — pending (to be run after YAML edit committed)
- `python3 tools/build_lineage_cards.py` — pending
- `python3 tools/build_docs.py` — pending
- YAML == JSON byte-equivalent — pending

---

## Drift finding: b2b-pup-drift-1

_See `docs/validation/2026-05-10-b2b-pup-drift-1.md` (created below)._

---

## Final verdict

**DRIFT-RECONCILED** — two identical locations (b2b-pup canonical + squarify local copy, verified byte-equivalent). One semantic-drift location (anish-tb-foster missing `conf` gate) documented as `variant_of` in YAML. No Pine source modification required. TV-firing deferred.

---

## Stage-7 followups

- [ ] Run `detection-plot-tv-firing` skill when at desk: load `LOGGER_B2B_PUP_v1.pine` and cross-check `S1: B2B Bull` fire bars against b2b-pup and squarify `det_b2bPUP`. Use symbol + timeframe from current session.
- [ ] Confirm whether anish-tb-foster's missing `conf` gate is intentional (intrabar use case) or a bug. If bug: fix `sPPBull` definition in that indicator — requires user approval.
- [ ] Designate b2b-pup vs hvd-pbj-ppd PUP canonical (noted as open question in `bible-input/extract-b2b-pup.yaml`).

---

## Provenance

- Skill version: detection-plot-validation v1.1
- Run started: 2026-05-10T00:00:00Z
- Run ended: 2026-05-10T00:00:00Z
- Wall-clock duration: ~8 minutes
- Subagents dispatched: Phase 1 = 0 (parent grepped directly, 3 files), Phase 2 = 0, Phase 3 = 0
- Parent agent: Claude Sonnet 4.6 (claude-sonnet-4-6)
