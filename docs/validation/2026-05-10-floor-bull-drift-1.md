# Drift finding — `FLOOR_BULL` — drift #1

_Generated by skill `detection-plot-validation` v1.0.0. Linked from parent report at `docs/validation/2026-05-10-floor-bull.md`._

## Summary

| Field | Value |
|---|---|
| Drift kind | semantic-drift |
| Locations | `hvd-pbj-ppd::FLOOR_BULL` (anyBullFloor) ↔ `squarify::S4_FLOOR` (floor_sq) |
| Severity | actionable |
| Action required | designate-canonical — YAML already lists hvd-pbj-ppd::FLOOR_BULL as canonical; squarify::S4_FLOOR is a confirmed ENRICHED VARIANT with additional gates (oneOfThese + cb1_pass_floor). User must ratify canonical designation. |

## Location A — `hvd-pbj-ppd::FLOOR_BULL` (anyBullFloor)

```
File:        hvd-pbj-ppd/versions/HVDPBJPPD_2026-05-10_THE_ONLY_ONE.pine
Line range:  L1239–L1241
Pine variable: anyBullFloor
Raw definition:
  bool bull_hw_slot = sigBullRVOL1x or sigGrandSlam or sigWTC or sigHiroshima or (sigNagasaki and nag_dir_bull)
  bool anyBullFloor = conf and bull_pp and sigBullPBJ and bull_hw_slot

Normalized (alpha-renamed):
  h_vol_slot = v1 OR v2 OR v3 OR v4 OR (v5 AND dir_bull)
  b_floor = CONF AND pp_bull AND pbj_bull AND h_vol_slot

Helpers used:
  bull_pp (L897): bull_cnt >= pp_min_count AND bull_has_floor_gravity AND bull_state > 0
  bull_hw_slot (L1239): RVOL1x OR GS OR WTC OR Hiro OR (Nag AND nag_dir_bull)
  sigBullPBJ: defined upstream (PBJ bull gate)

IPSF inputs used:
  show_BullFloor = input.bool(true,...) — toggles fire_BullFloor, NOT anyBullFloor itself
  pp_min_count — IPSF (governs Ping-Pong state threshold)
```

## Location B — `squarify::S4_FLOOR` (floor_sq)

```
File:        squarify/versions/SQUARIFY_46_v2_2026-05-04.pine
Line range:  L1349–L1351 (anyBullFloor, shared), L2109 (oneOfThese), L2137 (cb1_pass_floor), L2156 (floor_sq def)
Pine variable: floor_sq
Raw definition:
  bool bull_hw_slot = sigBullRVOL1x or sigGrandSlam or sigWTC or sigHiroshima or (sigNagasaki and nag_dir_bull)  [L1349 — byte-identical to A]
  bool anyBullFloor = conf and bull_pp and sigBullPBJ and bull_hw_slot  [L1351 — byte-identical to A]
  bool oneOfThese = sigP21BullUUUU or sigP21BullUUU or sigUUBull or sigOmegaLongA or det_bullNapalmCons
                   or det_b2bBullNapalm or csNew3_Bull or (csNew1_Bull or csNew2_Bull) or sigWMD  [L2109]
  bool cb1_pass_floor = f_cb(cb1_uu, cb1_combo, cb1_d9, cb1_pup, false, cb1_l1, cb1_wmd)  [L2137]
  bool floor_sq = anyBullFloor and oneOfThese and cb1_pass_floor  [L2156]

Normalized (alpha-renamed):
  h_vol_slot = v1 OR v2 OR v3 OR v4 OR (v5 AND dir_bull)
  b_base_floor = CONF AND pp_bull AND pbj_bull AND h_vol_slot   [= Location A — identical]
  one_of = UUUU OR UUU OR UU OR OmegaA OR NapalCons OR B2BNapalm OR CS3 OR (CS1 OR CS2) OR WMD
  cb1_f = checkbox_gate(uu, combo, d9, pup, FALSE, l1, wmd)    [PBJ excluded from checkbox]
  b_floor = b_base_floor AND one_of AND cb1_f                  [EXTRA gates not in Location A]

Helpers used:
  anyBullFloor (L1351): byte-identical to Location A
  oneOfThese (L2109): UUUU/UUU/UU/OmegaA/NapalmCons/B2BNapalm/CS3/(CS1 OR CS2)/WMD — NOT in Location A
  cb1_pass_floor (L2137): IPSF checkbox filter — NOT in Location A

IPSF inputs used:
  cb1_uu, cb1_combo, cb1_d9, cb1_pup, cb1_l1, cb1_wmd — all input.bool(); user-tunable checkboxes
  en_4 — input.bool toggle for S4 on chart (gates plotshape only, not floor_sq computation)
```

## Side-by-side diff

```diff
--- A: hvd-pbj-ppd::FLOOR_BULL (anyBullFloor)
+++ B: squarify::S4_FLOOR (floor_sq)
@@ composed boolean @@
- b_floor = CONF AND pp_bull AND pbj_bull AND h_vol_slot
+ b_floor = CONF AND pp_bull AND pbj_bull AND h_vol_slot AND one_of_these AND cb1_pass_floor
```

The base (`anyBullFloor`) is byte-identical in both files (L1239–L1351 in hvd-pbj-ppd; L1349–L1351 in squarify). Squarify **gates that base boolean with two additional enrichment conditions** — `oneOfThese` (a multi-signal OR of UUUU/UUU/UU/OmegaA/NapalCons/B2BNapalm/CS3/CS1orCS2/WMD) and `cb1_pass_floor` (user-configurable checkbox filter). This makes `floor_sq` strictly a subset of `anyBullFloor`.

## Classification evidence

Predicate matched: **"operators / operand structure differ"** → `semantic drift`.

Decision tree path:
1. Normalized booleans identical byte-for-byte? **NO** — squarify adds two AND gates.
2. Differ ONLY in variable names? **NO** — additional operands present.
3. Both use `input.*` for the differing parameter? **PARTIAL** — `cb1_*` are all `input.bool()` (IPSF); `oneOfThese` is hardcoded structure.
4. Semantic drift confirmed: the boolean structure differs (different operand count, different gates).

**Sub-classification: enrichment variant, not corruption.** This is an intentional design pattern in the Squarify suite — FLOOR_BULL acts as a base; `floor_sq` enriches it with a quality gate (`oneOfThese`) and a user-tunable filter (`cb1_pass_floor`). The base signal in hvd-pbj-ppd produces more fires (lower precision, higher recall); `floor_sq` in Squarify fires a strict subset.

## Proposed action

`designate-canonical` — Per YAML, `hvd-pbj-ppd::FLOOR_BULL` is canonical. `squarify::S4_FLOOR` should be tagged `variant_of: hvd-pbj-ppd::FLOOR_BULL` in `bible-input/extract-squarify.yaml` with a note: "enriched variant — adds oneOfThese quality gate + cb1_pass_floor checkbox filter." No Pine source change required.

## User approval needed?

YES — semantic-drift verdict with canonical designation. YAML edit in extract-squarify.yaml is low-risk (adding `variant_of:` field), but user must ratify the canonical direction before applying.
