# Redundancy & Drift

_Auto-generated by `tools/build_docs.py` from `data/indicators.yaml`. Edit the YAML; re-run the script._

Two tables. **(a)** name collisions — same primitive name appearing under two or more provenance namespaces. **(b)** internal-implementation drift — same canonical name with different underlying Pine math across files.

## (a) Name collisions across indicators

**174 colliding names.** Each row groups all canonical IDs that share an unqualified name.

| Short name | Occurrences | Verdict | Recommendation |
|---|---|---|---|
| `2nd Floor` | `hvd-pbj-ppd-1939::2nd Floor` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::2nd Floor` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Aggregated Alert` | `hvd-pbj-ppd-1939::Aggregated Alert` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Aggregated Alert` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Alpha Strike Bear` | `hvd-pbj-ppd-1939::Alpha Strike Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Alpha Strike Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Alpha Strike Bull` | `hvd-pbj-ppd-1939::Alpha Strike Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Alpha Strike Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `AnishBear` | `anish-50-1st-combo::AnishBear` (in anish-50-1st-combo); `anish-tb-foster::AnishBear` (in anish-tb-foster); `fauna-shifu::AnishBear` (in fauna-shifu) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `AnishBull` | `anish-50-1st-combo::AnishBull` (in anish-50-1st-combo); `anish-tb-foster::AnishBull` (in anish-tb-foster); `fauna-shifu::AnishBull` (in fauna-shifu) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `B2B_PPD` | `squarify::B2B_PPD` (in squarify); `b2b-pup::B2B_PPD` (in b2b-pup) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `B2B_PUP` | `squarify::B2B_PUP` (in squarify); `b2b-pup::B2B_PUP` (in b2b-pup) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Bear UU` | `hvd-pbj-ppd-1939::Bear UU` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Bear UU` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Bear UUU` | `hvd-pbj-ppd-1939::Bear UUU` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Bear UUU` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Bear UUUU` | `hvd-pbj-ppd-1939::Bear UUUU` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Bear UUUU` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `BearPB` | `anish-tb-foster::BearPB` (in anish-tb-foster); `yin-yang-displacement-pbj::BearPB` (in yin-yang-displacement-pbj) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `BearPBJ` | `anish-tb-foster::BearPBJ` (in anish-tb-foster); `yin-yang-displacement-pbj::BearPBJ` (in yin-yang-displacement-pbj) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Breakdown` | `yin-yang-displacement-pbj::Breakdown` (in yin-yang-displacement-pbj); `yin-yang-og-mofo::Breakdown` (in yin-yang-og-mofo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Breakout` | `yin-yang-displacement-pbj::Breakout` (in yin-yang-displacement-pbj); `yin-yang-og-mofo::Breakout` (in yin-yang-og-mofo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Bull UU` | `hvd-pbj-ppd-1939::Bull UU` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Bull UU` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Bull UUU` | `hvd-pbj-ppd-1939::Bull UUU` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Bull UUU` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Bull UUUU` | `hvd-pbj-ppd-1939::Bull UUUU` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Bull UUUU` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `BullPB` | `anish-tb-foster::BullPB` (in anish-tb-foster); `yin-yang-displacement-pbj::BullPB` (in yin-yang-displacement-pbj) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `BullPBJ` | `anish-tb-foster::BullPBJ` (in anish-tb-foster); `yin-yang-displacement-pbj::BullPBJ` (in yin-yang-displacement-pbj) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `CC Bear` | `hvd-pbj-ppd-1939::CC Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::CC Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `CC Bull` | `hvd-pbj-ppd-1939::CC Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::CC Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `CS1 FVG Bear` | `hvd-pbj-ppd-1939::CS1 FVG Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::CS1 FVG Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `CS1 FVG Bull` | `hvd-pbj-ppd-1939::CS1 FVG Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::CS1 FVG Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `CS2 MAT Bear` | `hvd-pbj-ppd-1939::CS2 MAT Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::CS2 MAT Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `CS2 MAT Bull` | `hvd-pbj-ppd-1939::CS2 MAT Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::CS2 MAT Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `CS3 Unified Bear` | `hvd-pbj-ppd-1939::CS3 Unified Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::CS3 Unified Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `CS3 Unified Bull` | `hvd-pbj-ppd-1939::CS3 Unified Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::CS3 Unified Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `DISP_BEAR` | `hvd-pbj-ppd::DISP_BEAR` (in hvd-pbj-ppd); `ultra-combo::DISP_BEAR` (in ultra-combo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `DISP_BULL` | `hvd-pbj-ppd::DISP_BULL` (in hvd-pbj-ppd); `ultra-combo::DISP_BULL` (in ultra-combo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Disp Bear 2+` | `hvd-pbj-ppd-1939::Disp Bear 2+` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Disp Bear 2+` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Disp Bear 3+` | `hvd-pbj-ppd-1939::Disp Bear 3+` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Disp Bear 3+` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Disp Bull 2+` | `hvd-pbj-ppd-1939::Disp Bull 2+` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Disp Bull 2+` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Disp Bull 3+` | `hvd-pbj-ppd-1939::Disp Bull 3+` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Disp Bull 3+` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `E3_BEAR` | `squarify::E3_BEAR` (in squarify); `ultra-combo::E3_BEAR` (in ultra-combo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `E3_BULL` | `squarify::E3_BULL` (in squarify); `ultra-combo::E3_BULL` (in ultra-combo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `F2_BEAR` | `squarify::F2_BEAR` (in squarify); `ultra-combo::F2_BEAR` (in ultra-combo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `F2_BULL` | `squarify::F2_BULL` (in squarify); `ultra-combo::F2_BULL` (in ultra-combo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `FAUNA_BEAR` | `b2b-pup::FAUNA_BEAR` (in b2b-pup); `fauna-shifu::FAUNA_BEAR` (in fauna-shifu); `heavy-weapons-8fvg-matrix::FAUNA_BEAR` (in heavy-weapons-8fvg-matrix); `heavy-uncap::FAUNA_BEAR` (in heavy-uncap); `ul… | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `FAUNA_BULL` | `b2b-pup::FAUNA_BULL` (in b2b-pup); `fauna-shifu::FAUNA_BULL` (in fauna-shifu); `heavy-weapons-8fvg-matrix::FAUNA_BULL` (in heavy-weapons-8fvg-matrix); `heavy-uncap::FAUNA_BULL` (in heavy-uncap); `ul… | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `FC_BEAR` | `squarify::FC_BEAR` (in squarify); `ultra-combo::FC_BEAR` (in ultra-combo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `FC_BULL` | `squarify::FC_BULL` (in squarify); `ultra-combo::FC_BULL` (in ultra-combo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `FOXTROT_BEAR` | `fauna-shifu::FOXTROT_BEAR` (in fauna-shifu); `hvd-pbj-ppd::FOXTROT_BEAR` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `FOXTROT_BULL` | `fauna-shifu::FOXTROT_BULL` (in fauna-shifu); `hvd-pbj-ppd::FOXTROT_BULL` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `FVG_BEAR_RAW` | `hv-fvg-gz1-og::FVG_BEAR_RAW` (in hv-fvg-gz1-og); `proximity-gzi-hv::FVG_BEAR_RAW` (in proximity-gzi-hv) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `FVG_BULL_RAW` | `hv-fvg-gz1-og::FVG_BULL_RAW` (in hv-fvg-gz1-og); `proximity-gzi-hv::FVG_BULL_RAW` (in proximity-gzi-hv) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Floor` | `hvd-pbj-ppd-1939::Floor` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Floor` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Foster` | `anish-50-1st-combo::Foster` (in anish-50-1st-combo); `anish-tb-foster::Foster` (in anish-tb-foster) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Foxtrot Bear` | `hvd-pbj-ppd-1939::Foxtrot Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Foxtrot Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Foxtrot Bull` | `hvd-pbj-ppd-1939::Foxtrot Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Foxtrot Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `GOLF_BEAR` | `fauna-shifu::GOLF_BEAR` (in fauna-shifu); `hvd-pbj-ppd::GOLF_BEAR` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `GOLF_BULL` | `fauna-shifu::GOLF_BULL` (in fauna-shifu); `hvd-pbj-ppd::GOLF_BULL` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `GRAND_SLAM` | `heavy-pentagon::GRAND_SLAM` (in heavy-pentagon); `heavy-weapons-8fvg-matrix::GRAND_SLAM` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `GZI_BEAR` | `hv-fvg-gz1-og::GZI_BEAR` (in hv-fvg-gz1-og); `proximity-gzi-hv::GZI_BEAR` (in proximity-gzi-hv) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `GZI_BULL` | `hv-fvg-gz1-og::GZI_BULL` (in hv-fvg-gz1-og); `proximity-gzi-hv::GZI_BULL` (in proximity-gzi-hv) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Golf Bear` | `hvd-pbj-ppd-1939::Golf Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Golf Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Golf Bull` | `hvd-pbj-ppd-1939::Golf Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Golf Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `GrandSlam` | `b2b-pup::GrandSlam` (in b2b-pup); `fauna-shifu::GrandSlam` (in fauna-shifu) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HEV` | `hv-ladder-100-to-1k::HEV` (in hv-ladder-100-to-1k); `hv-ladder-50-to-1k::HEV` (in hv-ladder-50-to-1k); `hvd-pbj-ppd::HEV` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HIROSHIMA` | `heavy-pentagon::HIROSHIMA` (in heavy-pentagon); `heavy-weapons-8fvg-matrix::HIROSHIMA` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HV1000` | `hv-ladder-100-to-1k::HV1000` (in hv-ladder-100-to-1k); `hv-ladder-50-to-1k::HV1000` (in hv-ladder-50-to-1k); `hvd-pbj-ppd::HV1000` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HV150` | `hv-ladder-50-to-1k::HV150` (in hv-ladder-50-to-1k); `hvd-pbj-ppd::HV150` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HV500` | `hv-ladder-100-to-1k::HV500` (in hv-ladder-100-to-1k); `hv-ladder-50-to-1k::HV500` (in hv-ladder-50-to-1k); `hvd-pbj-ppd::HV500` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HVD_BEAR` | `hvd-pbj-ppd-1939::HVD_BEAR` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::HVD_BEAR` (in hvd-pbj-ppd-2246-masterdir); `hvd-pbj-ppd::HVD_BEAR` (in hvd-pbj-ppd); `squarify::HVD_BEAR` (in squarify) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HVD_BULL` | `hvd-pbj-ppd-1939::HVD_BULL` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::HVD_BULL` (in hvd-pbj-ppd-2246-masterdir); `hvd-pbj-ppd::HVD_BULL` (in hvd-pbj-ppd); `squarify::HVD_BULL` (in squarify) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HV_BEAR` | `hv-fvg-gz1-og::HV_BEAR` (in hv-fvg-gz1-og); `proximity-gzi-hv::HV_BEAR` (in proximity-gzi-hv) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HV_BULL` | `hv-fvg-gz1-og::HV_BULL` (in hv-fvg-gz1-og); `proximity-gzi-hv::HV_BULL` (in proximity-gzi-hv) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HW Bear` | `hvd-pbj-ppd-1939::HW Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::HW Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HW Bull` | `hvd-pbj-ppd-1939::HW Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::HW Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `HotSpot` | `hv-ladder-100-to-1k::HotSpot` (in hv-ladder-100-to-1k); `hv-ladder-50-to-1k::HotSpot` (in hv-ladder-50-to-1k) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `KRATOS` | `heavy-pentagon::KRATOS` (in heavy-pentagon); `heavy-weapons-8fvg-matrix::KRATOS` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Kratos` | `b2b-pup::Kratos` (in b2b-pup); `fauna-shifu::Kratos` (in fauna-shifu) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `LONG_1` | `heavy-uncap::LONG_1` (in heavy-uncap); `heavy-weapons-8fvg-matrix::LONG_1` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `LONG_2` | `heavy-uncap::LONG_2` (in heavy-uncap); `heavy-weapons-8fvg-matrix::LONG_2` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `LSC Bear` | `hvd-pbj-ppd-1939::LSC Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::LSC Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `LSC Bull` | `hvd-pbj-ppd-1939::LSC Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::LSC Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `MOAB` | `b2b-pup::MOAB` (in b2b-pup); `fauna-shifu::MOAB` (in fauna-shifu); `heavy-pentagon::MOAB` (in heavy-pentagon); `heavy-weapons-8fvg-matrix::MOAB` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `NAGASAKI` | `fauna-shifu::NAGASAKI` (in fauna-shifu); `heavy-pentagon::NAGASAKI` (in heavy-pentagon); `heavy-weapons-8fvg-matrix::NAGASAKI` (in heavy-weapons-8fvg-matrix); `hvd-pbj-ppd::NAGASAKI` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `NEO_BEAR` | `heavy-weapons-8fvg-matrix::NEO_BEAR` (in heavy-weapons-8fvg-matrix); `squarify::NEO_BEAR` (in squarify) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `NEO_BULL` | `heavy-weapons-8fvg-matrix::NEO_BULL` (in heavy-weapons-8fvg-matrix); `squarify::NEO_BULL` (in squarify) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Nagasaki` | `b2b-pup::Nagasaki` (in b2b-pup); `vob-asym-ummmm::Nagasaki` (in vob-asym-ummmm-masterdir); `vob-asym::Nagasaki` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `OD Bear` | `hvd-pbj-ppd-1939::OD Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::OD Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `OD Bull` | `hvd-pbj-ppd-1939::OD Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::OD Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Omega Long` | `hvd-pbj-ppd-1939::Omega Long` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Omega Long` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PAF PPD B2B` | `hvd-pbj-ppd-1939::PAF PPD B2B` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::PAF PPD B2B` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PAF PUP B2B` | `hvd-pbj-ppd-1939::PAF PUP B2B` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::PAF PUP B2B` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PAF_BEAR` | `fauna-shifu::PAF_BEAR` (in fauna-shifu); `hvd-pbj-ppd::PAF_BEAR` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PAF_BULL` | `fauna-shifu::PAF_BULL` (in fauna-shifu); `hvd-pbj-ppd::PAF_BULL` (in hvd-pbj-ppd) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PBJ_BEAR` | `fauna-shifu::PBJ_BEAR` (in fauna-shifu); `hvd-pbj-ppd-1939::PBJ_BEAR` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::PBJ_BEAR` (in hvd-pbj-ppd-2246-masterdir); `hvd-pbj-ppd::PBJ_BEAR` (in hvd-p… | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PBJ_BULL` | `fauna-shifu::PBJ_BULL` (in fauna-shifu); `hvd-pbj-ppd-1939::PBJ_BULL` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::PBJ_BULL` (in hvd-pbj-ppd-2246-masterdir); `hvd-pbj-ppd::PBJ_BULL` (in hvd-p… | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PB_BEAR` | `fauna-shifu::PB_BEAR` (in fauna-shifu); `hvd-pbj-ppd-1939::PB_BEAR` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::PB_BEAR` (in hvd-pbj-ppd-2246-masterdir); `hvd-pbj-ppd::PB_BEAR` (in hvd-pbj-p… | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PB_BULL` | `fauna-shifu::PB_BULL` (in fauna-shifu); `hvd-pbj-ppd-1939::PB_BULL` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::PB_BULL` (in hvd-pbj-ppd-2246-masterdir); `hvd-pbj-ppd::PB_BULL` (in hvd-pbj-p… | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PENTAGON` | `heavy-pentagon::PENTAGON` (in heavy-pentagon); `heavy-weapons-8fvg-matrix::PENTAGON` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PPBear` | `anish-50-1st-combo::PPBear` (in anish-50-1st-combo); `anish-tb-foster::PPBear` (in anish-tb-foster) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PPBull` | `anish-50-1st-combo::PPBull` (in anish-50-1st-combo); `anish-tb-foster::PPBull` (in anish-tb-foster) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PPD` | `b2b-pup::PPD` (in b2b-pup); `fauna-shifu::PPD` (in fauna-shifu); `hvd-pbj-ppd-1939::PPD` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::PPD` (in hvd-pbj-ppd-2246-masterdir); `hvd-pbj-ppd::PPD` … | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `PUP` | `b2b-pup::PUP` (in b2b-pup); `fauna-shifu::PUP` (in fauna-shifu); `hvd-pbj-ppd-1939::PUP` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::PUP` (in hvd-pbj-ppd-2246-masterdir); `hvd-pbj-ppd::PUP` … | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Penthouse` | `hvd-pbj-ppd-1939::Penthouse` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Penthouse` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `RVOL_1X_BEAR` | `heavy-pentagon::RVOL_1X_BEAR` (in heavy-pentagon); `heavy-weapons-8fvg-matrix::RVOL_1X_BEAR` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `RVOL_1X_BULL` | `heavy-pentagon::RVOL_1X_BULL` (in heavy-pentagon); `heavy-weapons-8fvg-matrix::RVOL_1X_BULL` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `ResistanceRejection` | `yin-yang-displacement-pbj::ResistanceRejection` (in yin-yang-displacement-pbj); `yin-yang-og-mofo::ResistanceRejection` (in yin-yang-og-mofo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Rooftop` | `hvd-pbj-ppd-1939::Rooftop` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Rooftop` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SAAB` | `b2b-pup::SAAB` (in b2b-pup); `fauna-shifu::SAAB` (in fauna-shifu); `heavy-pentagon::SAAB` (in heavy-pentagon); `heavy-weapons-8fvg-matrix::SAAB` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SD Bear` | `hvd-pbj-ppd-1939::SD Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::SD Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SD Bull` | `hvd-pbj-ppd-1939::SD Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::SD Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SHORT_1` | `heavy-uncap::SHORT_1` (in heavy-uncap); `heavy-weapons-8fvg-matrix::SHORT_1` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SHORT_2` | `heavy-uncap::SHORT_2` (in heavy-uncap); `heavy-weapons-8fvg-matrix::SHORT_2` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SUPER Bear` | `hvd-pbj-ppd-1939::SUPER Bear` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::SUPER Bear` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SUPER Bull` | `hvd-pbj-ppd-1939::SUPER Bull` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::SUPER Bull` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SuperPPD` | `anish-50-1st-combo::SuperPPD` (in anish-50-1st-combo); `anish-tb-foster::SuperPPD` (in anish-tb-foster) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SuperPup` | `anish-50-1st-combo::SuperPup` (in anish-50-1st-combo); `anish-tb-foster::SuperPup` (in anish-tb-foster) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SupportRejection` | `yin-yang-displacement-pbj::SupportRejection` (in yin-yang-displacement-pbj); `yin-yang-og-mofo::SupportRejection` (in yin-yang-og-mofo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SwingHigh` | `yin-yang-displacement-pbj::SwingHigh` (in yin-yang-displacement-pbj); `yin-yang-og-mofo::SwingHigh` (in yin-yang-og-mofo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `SwingLow` | `yin-yang-displacement-pbj::SwingLow` (in yin-yang-displacement-pbj); `yin-yang-og-mofo::SwingLow` (in yin-yang-og-mofo) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T1-buy` | `vob-single-sens::T1-buy` (in vob-single-sens-no-tables); `vob-single-sens::T1-buy` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T1-sell` | `vob-single-sens::T1-sell` (in vob-single-sens-no-tables); `vob-single-sens::T1-sell` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T2-buy` | `vob-single-sens::T2-buy` (in vob-single-sens-no-tables); `vob-single-sens::T2-buy` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T2-sell` | `vob-single-sens::T2-sell` (in vob-single-sens-no-tables); `vob-single-sens::T2-sell` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3-buy` | `vob-single-sens::T3-buy` (in vob-single-sens-no-tables); `vob-single-sens::T3-buy` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3-sell` | `vob-single-sens::T3-sell` (in vob-single-sens-no-tables); `vob-single-sens::T3-sell` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3_bloomberg_alert` | `vob-asym-ummmm::T3_bloomberg_alert` (in vob-asym-ummmm-masterdir); `vob-asym::T3_bloomberg_alert` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3a-buy` | `vob-asym-ummmm::T3a-buy` (in vob-asym-ummmm-masterdir); `vob-asym::T3a-buy` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3a-sell` | `vob-asym-ummmm::T3a-sell` (in vob-asym-ummmm-masterdir); `vob-asym::T3a-sell` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3b-buy` | `vob-asym-ummmm::T3b-buy` (in vob-asym-ummmm-masterdir); `vob-asym::T3b-buy` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3b-sell` | `vob-asym-ummmm::T3b-sell` (in vob-asym-ummmm-masterdir); `vob-asym::T3b-sell` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3c-buy` | `vob-asym-ummmm::T3c-buy` (in vob-asym-ummmm-masterdir); `vob-asym::T3c-buy` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3c-sell` | `vob-asym-ummmm::T3c-sell` (in vob-asym-ummmm-masterdir); `vob-asym::T3c-sell` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3d-buy` | `vob-asym-ummmm::T3d-buy` (in vob-asym-ummmm-masterdir); `vob-asym::T3d-buy` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3d-sell` | `vob-asym-ummmm::T3d-sell` (in vob-asym-ummmm-masterdir); `vob-asym::T3d-sell` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3e-buy` | `vob-asym-ummmm::T3e-buy` (in vob-asym-ummmm-masterdir); `vob-asym::T3e-buy` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3e-sell` | `vob-asym-ummmm::T3e-sell` (in vob-asym-ummmm-masterdir); `vob-asym::T3e-sell` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3f-buy` | `vob-asym-ummmm::T3f-buy` (in vob-asym-ummmm-masterdir); `vob-asym::T3f-buy` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `T3f-sell` | `vob-asym-ummmm::T3f-sell` (in vob-asym-ummmm-masterdir); `vob-asym::T3f-sell` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `TB` | `ultra-combo::TB` (in ultra-combo); `anish-50-1st-combo::TB` (in anish-50-1st-combo); `anish-tb-foster::TB` (in anish-tb-foster) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `TRINITY_BEAR` | `heavy-weapons-8fvg-matrix::TRINITY_BEAR` (in heavy-weapons-8fvg-matrix); `squarify::TRINITY_BEAR` (in squarify) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `TRINITY_BULL` | `heavy-weapons-8fvg-matrix::TRINITY_BULL` (in heavy-weapons-8fvg-matrix); `squarify::TRINITY_BULL` (in squarify) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `Triple AND (Pipeline D)` | `hvd-pbj-ppd-1939::Triple AND (Pipeline D)` (in hvd-pbj-ppd-1939-masterdir); `hvd-pbj-ppd-2246::Triple AND (Pipeline D)` (in hvd-pbj-ppd-2246-masterdir) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `VolumeHierarchy` | `hv-ladder-100-to-1k::VolumeHierarchy` (in hv-ladder-100-to-1k); `hv-ladder-50-to-1k::VolumeHierarchy` (in hv-ladder-50-to-1k) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `WTC` | `b2b-pup::WTC` (in b2b-pup); `heavy-pentagon::WTC` (in heavy-pentagon); `heavy-weapons-8fvg-matrix::WTC` (in heavy-weapons-8fvg-matrix) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `ZONE_FORMATION_bloomberg_alert` | `vob-asym-ummmm::ZONE_FORMATION_bloomberg_alert` (in vob-asym-ummmm-masterdir); `vob-asym::ZONE_FORMATION_bloomberg_alert` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `alert_tier1_buy_payload` | `vob-single-sens::alert_tier1_buy_payload` (in vob-single-sens-no-tables); `vob-single-sens::alert_tier1_buy_payload` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `alert_tier1_sell_payload` | `vob-single-sens::alert_tier1_sell_payload` (in vob-single-sens-no-tables); `vob-single-sens::alert_tier1_sell_payload` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `alert_tier2_buy_payload` | `vob-single-sens::alert_tier2_buy_payload` (in vob-single-sens-no-tables); `vob-single-sens::alert_tier2_buy_payload` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `alert_tier2_sell_payload` | `vob-single-sens::alert_tier2_sell_payload` (in vob-single-sens-no-tables); `vob-single-sens::alert_tier2_sell_payload` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `alert_tier3_buy_payload` | `vob-single-sens::alert_tier3_buy_payload` (in vob-single-sens-no-tables); `vob-single-sens::alert_tier3_buy_payload` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `alert_tier3_sell_payload` | `vob-single-sens::alert_tier3_sell_payload` (in vob-single-sens-no-tables); `vob-single-sens::alert_tier3_sell_payload` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `any_t3` | `vob-asym-ummmm::any_t3` (in vob-asym-ummmm-masterdir); `vob-asym::any_t3` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `any_t3_alert` | `vob-asym-ummmm::any_t3_alert` (in vob-asym-ummmm-masterdir); `vob-asym::any_t3_alert` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `any_zone` | `vob-asym-ummmm::any_zone` (in vob-asym-ummmm-masterdir); `vob-asym::any_zone` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `any_zone_alert` | `vob-asym-ummmm::any_zone_alert` (in vob-asym-ummmm-masterdir); `vob-asym::any_zone_alert` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `bear_pool_total` | `vob-single-sens::bear_pool_total` (in vob-single-sens-no-tables); `vob-single-sens::bear_pool_total` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `bull_count / bear_count` | `vob-single-sens::bull_count / bear_count` (in vob-single-sens-no-tables); `vob-single-sens::bull_count / bear_count` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `bull_pool_total` | `vob-single-sens::bull_pool_total` (in vob-single-sens-no-tables); `vob-single-sens::bull_pool_total` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `comprehensive_emission_table` | `vob-asym-ummmm::comprehensive_emission_table` (in vob-asym-ummmm-masterdir); `vob-asym::comprehensive_emission_table` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `dom_bear_vol / dom_bear_pct` | `vob-single-sens::dom_bear_vol / dom_bear_pct` (in vob-single-sens-no-tables); `vob-single-sens::dom_bear_vol / dom_bear_pct` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `dom_bull_vol / dom_bull_pct` | `vob-single-sens::dom_bull_vol / dom_bull_pct` (in vob-single-sens-no-tables); `vob-single-sens::dom_bull_vol / dom_bull_pct` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `ema_trend_shadow` | `vob-single-sens::ema_trend_shadow` (in vob-single-sens-no-tables); `vob-single-sens::ema_trend_shadow` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `mutex_zone_lines` | `vob-asym-ummmm::mutex_zone_lines` (in vob-asym-ummmm-masterdir); `vob-asym::mutex_zone_lines` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `price_at_bull / price_at_bear` | `vob-single-sens::price_at_bull / price_at_bear` (in vob-single-sens-no-tables); `vob-single-sens::price_at_bull / price_at_bear` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `raw_tier_flags` | `vob-single-sens::raw_tier_flags` (in vob-single-sens-no-tables); `vob-single-sens::raw_tier_flags` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zA-bear` | `vob-asym-ummmm::zA-bear` (in vob-asym-ummmm-masterdir); `vob-asym::zA-bear` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zA-bull` | `vob-asym-ummmm::zA-bull` (in vob-asym-ummmm-masterdir); `vob-asym::zA-bull` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zB-bear` | `vob-asym-ummmm::zB-bear` (in vob-asym-ummmm-masterdir); `vob-asym::zB-bear` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zB-bull` | `vob-asym-ummmm::zB-bull` (in vob-asym-ummmm-masterdir); `vob-asym::zB-bull` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zC-bear` | `vob-asym-ummmm::zC-bear` (in vob-asym-ummmm-masterdir); `vob-asym::zC-bear` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zC-bull` | `vob-asym-ummmm::zC-bull` (in vob-asym-ummmm-masterdir); `vob-asym::zC-bull` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zD-bear` | `vob-asym-ummmm::zD-bear` (in vob-asym-ummmm-masterdir); `vob-asym::zD-bear` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zD-bull` | `vob-asym-ummmm::zD-bull` (in vob-asym-ummmm-masterdir); `vob-asym::zD-bull` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zE-bear` | `vob-asym-ummmm::zE-bear` (in vob-asym-ummmm-masterdir); `vob-asym::zE-bear` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zE-bull` | `vob-asym-ummmm::zE-bull` (in vob-asym-ummmm-masterdir); `vob-asym::zE-bull` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zF-bear` | `vob-asym-ummmm::zF-bear` (in vob-asym-ummmm-masterdir); `vob-asym::zF-bear` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zF-bull` | `vob-asym-ummmm::zF-bull` (in vob-asym-ummmm-masterdir); `vob-asym::zF-bull` (in vob-asym) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zone_drawing_bear` | `vob-single-sens::zone_drawing_bear` (in vob-single-sens-no-tables); `vob-single-sens::zone_drawing_bear` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |
| `zone_drawing_bull` | `vob-single-sens::zone_drawing_bull` (in vob-single-sens-no-tables); `vob-single-sens::zone_drawing_bull` (in vob-single-sens-with-tables) | drifted? — verify per-record | designate one canonical, mark others as variants in YAML |

## (b) Internal-implementation drift inside roots

Roots whose internal Pine implementation may have diverged across the indicators that NAME them. Detection method: byte-diff each root's source-line range across files.

Most-likely-drift candidates (per Stage-1 agent findings):

| Drift candidate | Notes | Stage-6 action |
|---|---|---|
| **PUP / PPD** | Defined in both `b2b-pup` (L73-L79 — locally redefined) and `hvd-pbj-ppd` (Engine 5). The 3%-bar + 10-bar opposite-volume rule is the same conceptually; the bible designates `b2b-pup::PUP` / `b2b-pup::PPD` as canonical pending Stage-1.5 byte-diff. | Stage 6.8 — pick canonical, mark other as alias |
| **PBJ / PB** | Defined in `hvd-pbj-ppd` (`HVDPBJPPD_2026-05-10_THE_ONLY_ONE.pine` is canonical per filename; predecessor `4.26.1244am` is `drifted-subset`). Also referenced in `b2b-pup` Engine D — verify identical implementation. | Stage 6.8 — confirm parity vs THE_ONLY_ONE |
| **RVOL family (SAAB, Kratos, RVOL1x, GS, MOAB, Pentagon, WTC, Hiroshima, Nagasaki)** | Canonical: `heavy-pentagon::*`. `heavy-combo-toggles` lifts verbatim (verified by extraction agent). `b2b-pup` Engine E and `hvd-pbj-ppd` USE pipeline have inline copies — verify drift. | Stage 6.8 — confirm parity vs heavy-pentagon |
| **GZI / HV / FVG (bull/bear)** | Canonical: `hv-fvg-gz1-og::*`. `proximity-gzi-hv` is a stripped variant (parity-checked OK in extraction agent: byte-identical except dashboard removal + title color). Other indicators may have drifted copies. | Stage 6.8 — confirm parity for non-stripped consumers |
| **FAUNA decomposition `(MB OR RE OR TA) AND NOT (GG OR TR OR ES OR GDR)`** | Locked in `ultra-combo` (L730-L732 bull, L746-L748 bear). Squarify and B2B PUP use FAUNA — verify they reference the same MB/RE/TA/GG/TR/ES/GDR primitives, not drifted variants. | Stage 6.8 — verify FAUNA component parity |
| **Pentagon vs anyBearPent** | Namespace collision: `Pentagon` is the RVOL Reg@Time tier; `anyBearPent` (in older HVDPBJPPD) is a bear platform mirror of FLOOR. Distinct concepts; same prefix. | Stage 6.1 — rename `anyBearPent` → `anyBear2F` |
| **Trinity vs Neo (Matrix family)** | Resolved by Squarify extraction (L1215-L1216): Neo = matrix-number AND FAUNA; Trinity = matrix-number AND NOT FAUNA AND directional close. Both feed `matrix_any_bull` → CS3/CS4 → Unified Combo. | No action — definition is now canonical in YAML |
| **TNT-OD v2 vs v3** | v3 features (Apex, Failed-Apex Flip, T1 Relay/Stack) live in `TNT_OD_v3.pine` (148KB) but the file currently designated as canonical (`TNT_Opening_Drive_OD_v3_2026-05-04.pine`, 122KB) is internally titled v2 and lacks them. Stage 6.4 must reconcile. | Stage 6.4 — pick the right canonical TNT-OD |

