# Lineage Card — `heavy-weapons-8fvg-matrix::COMBO_SET_1_BEAR`

_Auto-generated by tools/build_lineage_cards.py from data/indicators.yaml. Do not edit by hand._

**Top-level composite:** `heavy-weapons-8fvg-matrix::COMBO_SET_1_BEAR` &nbsp;&nbsp;`tier-2` &nbsp;&nbsp;`stage-5` &nbsp;&nbsp;`offset=-1`

**One-line:** Bearish GZI/HV FVG combo: bar[1] has valid body AND (bearGZI OR bearHV) AND (Kratos[1] OR Bear RVOL 1x[1] OR MOAB[1]). Fires on bar[0], offset=-1.

## Dependency expansion

```
● `heavy-weapons-8fvg-matrix::COMBO_SET_1_BEAR`  tier-2  offset=-1
  one-line: Bearish GZI/HV FVG combo: bar[1] has valid body AND (bearGZI OR bearHV) AND (Kratos[1] OR Bear RVOL 1x[1] OR MOAB[1]). F
  └── ROOT  `heavy-weapons-8fvg-matrix::BEAR_GZI`  offset=0  — Bearish FVG (high < low[2], close[1] < low[2], vertical gap > threshold) overlap
  └── ROOT  `heavy-weapons-8fvg-matrix::BEAR_HV`  offset=0  — Bearish FVG where prior bar volume is an all-time high (5000-bar / 252-bar / 63-
  └── ROOT  `heavy-weapons-8fvg-matrix::KRATOS`  offset=0  — Bearish bar with normalized price spike in the SAAB/Kratos band. LIFTED VERBATIM
  └── ROOT  `heavy-weapons-8fvg-matrix::RVOL_1X_BEAR`  offset=0  — Bearish bar with normalized price spike in the RVOL-1x band. LIFTED VERBATIM FRO
  └── ROOT  `heavy-weapons-8fvg-matrix::MOAB`  offset=0  — Bearish bar with normalized price spike at or above the MOAB threshold (top tier
```

## Root inventory (deduplicated leaves)

- `heavy-weapons-8fvg-matrix::BEAR_GZI` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::BEAR_HV` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::KRATOS` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::MOAB` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::RVOL_1X_BEAR` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`

## Offset ledger (for offset-bug triage)

- `heavy-weapons-8fvg-matrix::COMBO_SET_1_BEAR` → offset `-1`
-   `heavy-weapons-8fvg-matrix::BEAR_GZI` → offset `0`
-   `heavy-weapons-8fvg-matrix::BEAR_HV` → offset `0`
-   `heavy-weapons-8fvg-matrix::KRATOS` → offset `0`
-   `heavy-weapons-8fvg-matrix::RVOL_1X_BEAR` → offset `0`
-   `heavy-weapons-8fvg-matrix::MOAB` → offset `0`

## Decoupling proof (per the lifecycle)

- Each root calculated independently in stage 2 on this bar's confirmed close.
- Each root's offset applied in stage 3.
- Plots render in stage 4.
- Composites read root booleans only at stage 5; no composite inspects root internals.
- This composite (`heavy-weapons-8fvg-matrix::COMBO_SET_1_BEAR`) reads its operands by name, never reaching into their calculations.

## Common failure modes

If this composite fires (or fails to fire) unexpectedly, check in this order:

1. **Offset mismatch** between this top-level and a child composite (compare offset ledger above).
2. **Cross-indicator drift** on a shared root (see `docs/redundancy.md` — especially the PBJ / RVOL ladder / Unified Combo sections).
3. **Namespace collision** pulling in the wrong implementation (e.g. `Pentagon` RVOL tier vs `anyBearPent` platform).
4. **Parameter drift** — the same root may have different defaults across indicators (DISP σ-mult, GZI proximity, Long1/2 thresholds).
