# Lineage Card — `heavy-weapons-8fvg-matrix::COMBO_SET_2_BULL`

_Auto-generated by tools/build_lineage_cards.py from data/indicators.yaml. Do not edit by hand._

**Top-level composite:** `heavy-weapons-8fvg-matrix::COMBO_SET_2_BULL` &nbsp;&nbsp;`tier-2` &nbsp;&nbsp;`stage-5` &nbsp;&nbsp;`offset=-1`

**One-line:** Bullish GZI/HV FVG combo with time-regularized volume gate: bar[1] valid body AND (bullGZI OR bullHV) AND (Pentagon[1] if enabled, else WTC[1] OR Hiroshima[1] OR Nagasaki[1]). Fires on bar[0], offset=-1.

## Dependency expansion

```
● `heavy-weapons-8fvg-matrix::COMBO_SET_2_BULL`  tier-2  offset=-1
  one-line: Bullish GZI/HV FVG combo with time-regularized volume gate: bar[1] valid body AND (bullGZI OR bullHV) AND (Pentagon[1] i
  └── ROOT  `heavy-weapons-8fvg-matrix::BULL_GZI`  offset=0  — Bullish FVG (low > high[2], close[1] > high[2], vertical gap > threshold) overla
  └── ROOT  `heavy-weapons-8fvg-matrix::BULL_HV`  offset=0  — Bullish FVG where prior bar volume is an all-time high (5000-bar / 252-bar / 63-
  └── ROOT  `heavy-weapons-8fvg-matrix::PENTAGON`  offset=0  — Time-regularized relative volume ratio in the Pentagon band (1× to 2× the RVOL-1
  └── ROOT  `heavy-weapons-8fvg-matrix::WTC`  offset=0  — Time-regularized relative volume ratio in the WTC band (2× to Hiroshima threshol
  └── ROOT  `heavy-weapons-8fvg-matrix::HIROSHIMA`  offset=0  — Time-regularized relative volume ratio exceeds the Hiroshima threshold (top tier
  └── ROOT  `heavy-weapons-8fvg-matrix::NAGASAKI`  offset=0  — All-time high volume bar — current bar volume exceeds the running maximum since 
```

## Root inventory (deduplicated leaves)

- `heavy-weapons-8fvg-matrix::BULL_GZI` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::BULL_HV` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::HIROSHIMA` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::NAGASAKI` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::PENTAGON` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::WTC` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`

## Offset ledger (for offset-bug triage)

- `heavy-weapons-8fvg-matrix::COMBO_SET_2_BULL` → offset `-1`
-   `heavy-weapons-8fvg-matrix::BULL_GZI` → offset `0`
-   `heavy-weapons-8fvg-matrix::BULL_HV` → offset `0`
-   `heavy-weapons-8fvg-matrix::PENTAGON` → offset `0`
-   `heavy-weapons-8fvg-matrix::WTC` → offset `0`
-   `heavy-weapons-8fvg-matrix::HIROSHIMA` → offset `0`
-   `heavy-weapons-8fvg-matrix::NAGASAKI` → offset `0`

## Decoupling proof (per the lifecycle)

- Each root calculated independently in stage 2 on this bar's confirmed close.
- Each root's offset applied in stage 3.
- Plots render in stage 4.
- Composites read root booleans only at stage 5; no composite inspects root internals.
- This composite (`heavy-weapons-8fvg-matrix::COMBO_SET_2_BULL`) reads its operands by name, never reaching into their calculations.

## Common failure modes

If this composite fires (or fails to fire) unexpectedly, check in this order:

1. **Offset mismatch** between this top-level and a child composite (compare offset ledger above).
2. **Cross-indicator drift** on a shared root (see `docs/redundancy.md` — especially the PBJ / RVOL ladder / Unified Combo sections).
3. **Namespace collision** pulling in the wrong implementation (e.g. `Pentagon` RVOL tier vs `anyBearPent` platform).
4. **Parameter drift** — the same root may have different defaults across indicators (DISP σ-mult, GZI proximity, Long1/2 thresholds).
