# Lineage Card — `heavy-weapons-8fvg-matrix::COMBO_SET_1_BULL`

_Auto-generated by tools/build_lineage_cards.py from data/indicators.yaml. Do not edit by hand._

**Top-level composite:** `heavy-weapons-8fvg-matrix::COMBO_SET_1_BULL` &nbsp;&nbsp;`tier-2` &nbsp;&nbsp;`stage-5` &nbsp;&nbsp;`offset=-1`

**One-line:** Bullish GZI/HV FVG combo: bar[1] has valid body (>= 0.85 candle range) AND (bullGZI OR bullHV) AND (SAAB[1] OR Bull RVOL 1x[1] OR Grand Slam[1]). Fires on bar[0]. Offset=-1 since bar[1] is the FVG candle.

## Dependency expansion

```
● `heavy-weapons-8fvg-matrix::COMBO_SET_1_BULL`  tier-2  offset=-1
  one-line: Bullish GZI/HV FVG combo: bar[1] has valid body (>= 0.85 candle range) AND (bullGZI OR bullHV) AND (SAAB[1] OR Bull RVOL
  └── ROOT  `heavy-weapons-8fvg-matrix::BULL_GZI`  offset=0  — Bullish FVG (low > high[2], close[1] > high[2], vertical gap > threshold) overla
  └── ROOT  `heavy-weapons-8fvg-matrix::BULL_HV`  offset=0  — Bullish FVG where prior bar volume is an all-time high (5000-bar / 252-bar / 63-
  └── ROOT  `heavy-weapons-8fvg-matrix::SAAB`  offset=0  — Bullish bar with normalized price spike in the SAAB/Kratos band (0.56× to 1× the
  └── ROOT  `heavy-weapons-8fvg-matrix::RVOL_1X_BULL`  offset=0  — Bullish bar with normalized price spike in the RVOL-1x band (1× to GrandSlam thr
  └── ROOT  `heavy-weapons-8fvg-matrix::GRAND_SLAM`  offset=0  — Bullish bar with normalized price spike at or above the Grand Slam threshold (to
```

## Root inventory (deduplicated leaves)

- `heavy-weapons-8fvg-matrix::BULL_GZI` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::BULL_HV` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::GRAND_SLAM` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::RVOL_1X_BULL` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-weapons-8fvg-matrix::SAAB` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`

## Offset ledger (for offset-bug triage)

- `heavy-weapons-8fvg-matrix::COMBO_SET_1_BULL` → offset `-1`
-   `heavy-weapons-8fvg-matrix::BULL_GZI` → offset `0`
-   `heavy-weapons-8fvg-matrix::BULL_HV` → offset `0`
-   `heavy-weapons-8fvg-matrix::SAAB` → offset `0`
-   `heavy-weapons-8fvg-matrix::RVOL_1X_BULL` → offset `0`
-   `heavy-weapons-8fvg-matrix::GRAND_SLAM` → offset `0`

## Decoupling proof (per the lifecycle)

- Each root calculated independently in stage 2 on this bar's confirmed close.
- Each root's offset applied in stage 3.
- Plots render in stage 4.
- Composites read root booleans only at stage 5; no composite inspects root internals.
- This composite (`heavy-weapons-8fvg-matrix::COMBO_SET_1_BULL`) reads its operands by name, never reaching into their calculations.

## Common failure modes

If this composite fires (or fails to fire) unexpectedly, check in this order:

1. **Offset mismatch** between this top-level and a child composite (compare offset ledger above).
2. **Cross-indicator drift** on a shared root (see `docs/redundancy.md` — especially the PBJ / RVOL ladder / Unified Combo sections).
3. **Namespace collision** pulling in the wrong implementation (e.g. `Pentagon` RVOL tier vs `anyBearPent` platform).
4. **Parameter drift** — the same root may have different defaults across indicators (DISP σ-mult, GZI proximity, Long1/2 thresholds).
