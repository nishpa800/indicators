# Lineage Card — `ultra-combo::ROC_BULL`

_Auto-generated by tools/build_lineage_cards.py from data/indicators.yaml. Do not edit by hand._

**Top-level composite:** `ultra-combo::ROC_BULL` &nbsp;&nbsp;`tier-2` &nbsp;&nbsp;`stage-5` &nbsp;&nbsp;`offset=0`

**One-line:** Bull ROC composite: PBJ-bull AND ROC+WaveTrend dual-confirmation (b1+b4 OR b3+b2) AND any-bull HW AND green body

## Dependency expansion

```
● `ultra-combo::ROC_BULL`  tier-2  offset=0
  one-line: Bull ROC composite: PBJ-bull AND ROC+WaveTrend dual-confirmation (b1+b4 OR b3+b2) AND any-bull HW AND green body
  └── ROOT  `hvd-pbj-ppd::PBJ_BULL`  offset=0  — Pipeline-B bullish PBJ landing: a Supertrend buy_cross (Zoo VWMA-5 vs ATR-band S
  └── ROOT  `ultra-combo::ROC_BULL_PRIMITIVE`  offset=0  — Atomic ROC-bull crossover primitive: ta.crossover(roc_ema, 2.0). Pre-merge with 
  └── ROOT  `ultra-combo::LBWT_BULL_CROSS`  offset=0  — LazyBear WaveTrend bull cross: ld_xup = wt1 crosses over wt2 (with HLC3-EMA8/CI/
  └── `(any HW bull: heavy-pentagon::SAAB-style components are NOT used here — only RVOL family)` (unresolved — not in roots or composites)
```

## Root inventory (deduplicated leaves)

- `hvd-pbj-ppd::PBJ_BULL` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `ultra-combo::LBWT_BULL_CROSS` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `ultra-combo::ROC_BULL_PRIMITIVE` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`

## Offset ledger (for offset-bug triage)

- `ultra-combo::ROC_BULL` → offset `0`
-   `hvd-pbj-ppd::PBJ_BULL` → offset `0`
-   `ultra-combo::ROC_BULL_PRIMITIVE` → offset `0`
-   `ultra-combo::LBWT_BULL_CROSS` → offset `0`

## Decoupling proof (per the lifecycle)

- Each root calculated independently in stage 2 on this bar's confirmed close.
- Each root's offset applied in stage 3.
- Plots render in stage 4.
- Composites read root booleans only at stage 5; no composite inspects root internals.
- This composite (`ultra-combo::ROC_BULL`) reads its operands by name, never reaching into their calculations.

## Common failure modes

If this composite fires (or fails to fire) unexpectedly, check in this order:

1. **Offset mismatch** between this top-level and a child composite (compare offset ledger above).
2. **Cross-indicator drift** on a shared root (see `docs/redundancy.md` — especially the PBJ / RVOL ladder / Unified Combo sections).
3. **Namespace collision** pulling in the wrong implementation (e.g. `Pentagon` RVOL tier vs `anyBearPent` platform).
4. **Parameter drift** — the same root may have different defaults across indicators (DISP σ-mult, GZI proximity, Long1/2 thresholds).
