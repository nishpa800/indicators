# Lineage Card — `ultra-combo::FAUNA_BEAR`

_Auto-generated by tools/build_lineage_cards.py from data/indicators.yaml. Do not edit by hand._

**Top-level composite:** `ultra-combo::FAUNA_BEAR` &nbsp;&nbsp;`tier-2` &nbsp;&nbsp;`stage-5` &nbsp;&nbsp;`offset=0`

**One-line:** Bearish FAUNA: any of bear MB/RE/TA fires AND none of bear GG/TR/ES/GDR exclusion atoms fires

## Dependency expansion

```
● `ultra-combo::FAUNA_BEAR`  tier-2  offset=0
  one-line: Bearish FAUNA: any of bear MB/RE/TA fires AND none of bear GG/TR/ES/GDR exclusion atoms fires
  └── ROOT  `ultra-combo::MB_BEAR`  offset=0  — FAUNA Momentum-Body bear (mirror of MB_BULL)
  └── ROOT  `ultra-combo::RE_BEAR`  offset=0  — FAUNA Range-Expansion bear (mirror)
  └── ROOT  `ultra-combo::TA_BEAR`  offset=0  — FAUNA Trend-Acceleration bear (mirror)
  └── `NOT ultra-combo::GG_BEAR` (unresolved — not in roots or composites)
  └── `NOT ultra-combo::TR_BEAR` (unresolved — not in roots or composites)
  └── `NOT ultra-combo::ES_BEAR` (unresolved — not in roots or composites)
  └── `NOT ultra-combo::GDR_BEAR` (unresolved — not in roots or composites)
```

## Root inventory (deduplicated leaves)

- `ultra-combo::MB_BEAR` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `ultra-combo::RE_BEAR` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `ultra-combo::TA_BEAR` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`

## Offset ledger (for offset-bug triage)

- `ultra-combo::FAUNA_BEAR` → offset `0`
-   `ultra-combo::MB_BEAR` → offset `0`
-   `ultra-combo::RE_BEAR` → offset `0`
-   `ultra-combo::TA_BEAR` → offset `0`

## Decoupling proof (per the lifecycle)

- Each root calculated independently in stage 2 on this bar's confirmed close.
- Each root's offset applied in stage 3.
- Plots render in stage 4.
- Composites read root booleans only at stage 5; no composite inspects root internals.
- This composite (`ultra-combo::FAUNA_BEAR`) reads its operands by name, never reaching into their calculations.

## Common failure modes

If this composite fires (or fails to fire) unexpectedly, check in this order:

1. **Offset mismatch** between this top-level and a child composite (compare offset ledger above).
2. **Cross-indicator drift** on a shared root (see `docs/redundancy.md` — especially the PBJ / RVOL ladder / Unified Combo sections).
3. **Namespace collision** pulling in the wrong implementation (e.g. `Pentagon` RVOL tier vs `anyBearPent` platform).
4. **Parameter drift** — the same root may have different defaults across indicators (DISP σ-mult, GZI proximity, Long1/2 thresholds).
