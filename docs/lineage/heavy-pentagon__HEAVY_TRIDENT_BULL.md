# Lineage Card — `heavy-pentagon::HEAVY_TRIDENT_BULL`

_Auto-generated by tools/build_lineage_cards.py from data/indicators.yaml. Do not edit by hand._

**Top-level composite:** `heavy-pentagon::HEAVY_TRIDENT_BULL` &nbsp;&nbsp;`tier-1` &nbsp;&nbsp;`stage-5` &nbsp;&nbsp;`offset=0`

**One-line:** Heavy Trident fires bullish: all three RVOL axes co-occur — Nagasaki (P3) + any directional P1 + any neutral P2 — gated bullish.

## Dependency expansion

```
● `heavy-pentagon::HEAVY_TRIDENT_BULL`  tier-1  offset=0
  one-line: Heavy Trident fires bullish: all three RVOL axes co-occur — Nagasaki (P3) + any directional P1 + any neutral P2 — gated 
  └── ROOT  `heavy-pentagon::NAGASAKI`  offset=0  — All-time high volume bar — current bar volume exceeds the running max volume sin
  └── ROOT  `heavy-pentagon::RVOL_1X_BULL`  offset=0  — Bullish bar with normalized price spike in the RVOL-1x band (between RVOL-1x thr
  └── ROOT  `heavy-pentagon::GRAND_SLAM`  offset=0  — Bullish bar with normalized price spike at or above the Grand Slam / MOAB thresh
  └── ROOT  `heavy-pentagon::RVOL_1X_BEAR`  offset=0  — Bearish bar with normalized price spike in the RVOL-1x band.
  └── ROOT  `heavy-pentagon::MOAB`  offset=0  — Bearish bar with normalized price spike at or above the Grand Slam / MOAB thresh
  └── ROOT  `heavy-pentagon::PENTAGON`  offset=0  — Time-regularized relative volume ratio sits in the Pentagon band (between RVOL-1
  └── ROOT  `heavy-pentagon::WTC`  offset=0  — Time-regularized relative volume ratio sits in the WTC band (above WTC threshold
  └── ROOT  `heavy-pentagon::HIROSHIMA`  offset=0  — Time-regularized relative volume ratio exceeds the Hiroshima threshold (top tier
```

## Root inventory (deduplicated leaves)

- `heavy-pentagon::GRAND_SLAM` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-pentagon::HIROSHIMA` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-pentagon::MOAB` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-pentagon::NAGASAKI` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-pentagon::PENTAGON` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-pentagon::RVOL_1X_BEAR` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-pentagon::RVOL_1X_BULL` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `heavy-pentagon::WTC` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`

## Offset ledger (for offset-bug triage)

- `heavy-pentagon::HEAVY_TRIDENT_BULL` → offset `0`
-   `heavy-pentagon::NAGASAKI` → offset `0`
-   `heavy-pentagon::RVOL_1X_BULL` → offset `0`
-   `heavy-pentagon::GRAND_SLAM` → offset `0`
-   `heavy-pentagon::RVOL_1X_BEAR` → offset `0`
-   `heavy-pentagon::MOAB` → offset `0`
-   `heavy-pentagon::PENTAGON` → offset `0`
-   `heavy-pentagon::WTC` → offset `0`
-   `heavy-pentagon::HIROSHIMA` → offset `0`

## Decoupling proof (per the lifecycle)

- Each root calculated independently in stage 2 on this bar's confirmed close.
- Each root's offset applied in stage 3.
- Plots render in stage 4.
- Composites read root booleans only at stage 5; no composite inspects root internals.
- This composite (`heavy-pentagon::HEAVY_TRIDENT_BULL`) reads its operands by name, never reaching into their calculations.

## Common failure modes

If this composite fires (or fails to fire) unexpectedly, check in this order:

1. **Offset mismatch** between this top-level and a child composite (compare offset ledger above).
2. **Cross-indicator drift** on a shared root (see `docs/redundancy.md` — especially the PBJ / RVOL ladder / Unified Combo sections).
3. **Namespace collision** pulling in the wrong implementation (e.g. `Pentagon` RVOL tier vs `anyBearPent` platform).
4. **Parameter drift** — the same root may have different defaults across indicators (DISP σ-mult, GZI proximity, Long1/2 thresholds).
