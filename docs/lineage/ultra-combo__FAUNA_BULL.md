# Lineage Card — `ultra-combo::FAUNA_BULL`

_Auto-generated by tools/build_lineage_cards.py from data/indicators.yaml. Do not edit by hand._

**Top-level composite:** `ultra-combo::FAUNA_BULL` &nbsp;&nbsp;`tier-2` &nbsp;&nbsp;`stage-5` &nbsp;&nbsp;`offset=0`

**One-line:** Bullish FAUNA: any of MB/RE/TA fires AND none of the four exclusion atoms (GG/TR/ES/GDR) fires

## Dependency expansion

```
● `ultra-combo::FAUNA_BULL`  tier-2  offset=0
  one-line: Bullish FAUNA: any of MB/RE/TA fires AND none of the four exclusion atoms (GG/TR/ES/GDR) fires
  └── ROOT  `ultra-combo::MB_BULL`  offset=0  — FAUNA Momentum-Body bull: confirmed bullish bar with body > 1.6*ATR, body/range 
  └── ROOT  `ultra-combo::RE_BULL`  offset=0  — FAUNA Range-Expansion bull: confirmed bull bar with range > 2.2*ATR, close in to
  └── ROOT  `ultra-combo::TA_BULL`  offset=0  — FAUNA Trend-Acceleration bull: 50-SMA rising, |close-close[1]| > 1.6*avg|delta|1
  └── `NOT ultra-combo::GG_BULL` (unresolved — not in roots or composites)
  └── `NOT ultra-combo::TR_BULL` (unresolved — not in roots or composites)
  └── `NOT ultra-combo::ES_BULL` (unresolved — not in roots or composites)
  └── `NOT ultra-combo::GDR_BULL` (unresolved — not in roots or composites)
```

## Root inventory (deduplicated leaves)

- `ultra-combo::MB_BULL` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `ultra-combo::RE_BULL` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`
- `ultra-combo::TA_BULL` &nbsp;&nbsp;`stage-2` &nbsp;&nbsp;`offset=0`

## Offset ledger (for offset-bug triage)

- `ultra-combo::FAUNA_BULL` → offset `0`
-   `ultra-combo::MB_BULL` → offset `0`
-   `ultra-combo::RE_BULL` → offset `0`
-   `ultra-combo::TA_BULL` → offset `0`

## Decoupling proof (per the lifecycle)

- Each root calculated independently in stage 2 on this bar's confirmed close.
- Each root's offset applied in stage 3.
- Plots render in stage 4.
- Composites read root booleans only at stage 5; no composite inspects root internals.
- This composite (`ultra-combo::FAUNA_BULL`) reads its operands by name, never reaching into their calculations.

## Common failure modes

If this composite fires (or fails to fire) unexpectedly, check in this order:

1. **Offset mismatch** between this top-level and a child composite (compare offset ledger above).
2. **Cross-indicator drift** on a shared root (see `docs/redundancy.md` — especially the PBJ / RVOL ladder / Unified Combo sections).
3. **Namespace collision** pulling in the wrong implementation (e.g. `Pentagon` RVOL tier vs `anyBearPent` platform).
4. **Parameter drift** — the same root may have different defaults across indicators (DISP σ-mult, GZI proximity, Long1/2 thresholds).
