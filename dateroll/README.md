# `dateroll/` — date-roll experimental builds (Set B)

**Don't use this file as an index.** There is exactly ONE index for the whole suite —
both the tick-safe builds and these date-roll (` DR`) builds, with status + raw URLs:

➡️ **[`/TICK_FRIENDLY_INDEX.md`](../TICK_FRIENDLY_INDEX.md)**

What's in here: a ` DR`-labeled copy of every tick build so they load beside the current ones
for A/B. Only ULTRA 57, SQUARIFY v2 and SQUARIFY v3.1 differ in logic (date-rolled "yesterday"
via `f_firedPrevDay`, no bar-scan); the rest are logic-identical copies. All pass
`tools/check_plot_budget.sh`.
