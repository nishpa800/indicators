# CHANGELOG — HV NRA (volume-high detection ladder)

Versions are GENERATED by `build_hv_ladder.py` (one source of truth for the
repetitive per-tier blocks). Edit the generator, never the `.pine` by hand.

## 2026-06-29 — Add coarse top tiers (2000/3000/4000/4999)

`versions/HV_NRA_ladder_to_4999_2026-06-29.pine`

Same indicator as the 50-step ladder, with four coarse lookback tiers added on
top: **2000, 3000, 4000, 4999** (24 tiers total). Top is 4999 (not 5000) so
`ta.highest(volume,N)[1]` stays inside Pine's historical buffer. Everything else
is unchanged — same non-repainting architecture (`volume[1]` + `…[1]` +
`offset=-1`), same nested single-marker-per-bar priority, same plot==alert
pairing, same HEV all-time-high tier, same exports (now `signalStates[25]`).

## v2 — 2026-06-13 — Full 50-step ladder + Hot Spot

`versions/HV_NRA_50step_ladder_v2_2026-06-13.pine`

Densified the lookback ladder from the original sparse 50/150/250/500/1000 to a
full 50-step ladder (50,100,…,1000 = 20 tiers) and added the calendar-based Hot
Spot signal. Single marker per bar via nested-tier priority.
